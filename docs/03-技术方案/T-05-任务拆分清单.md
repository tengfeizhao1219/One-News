# T-05 任务拆分清单（阶段四开发任务）

> **任务**：T-05 | **协作**：技术负责人（任务拆解） + 项目经理（排期/资源/负责人） | **日期**：2026-07-30
> **输入**：T-04 技术评审纪要 §五（开发任务映射） + D-04 设计评审纪要
> **状态**：TL 侧拆解完成 → 待 PM 认领负责人、排期、资源

---

## 一、总览

| 阶段 | 任务数 | 预估总工时 | 建议并行策略 |
|------|:---:|:---:|------|
| 阶段四 开发实现 | 7 主任务 + 若干子任务 | 8–12d (1人) / 5–7d (2人并行) | 后端 B-02/B-06 先行 → 前端 B-01/B-03/B-04/B-05/B-07 跟进 |

---

## 二、任务拆分

### B-01 跨分类阅读引擎 🔴 大

| 子任务 | 描述 | 文件 | 预估 |
|--------|------|------|:---:|
| B-01.1 | 创建 `reading-engine.js` 状态机模块 | `pages/detail/reading-engine.js` | 1d |
| B-01.2 | 实现 mergedList 构建（并行 getNewsList × 5） | `detail.js` onLoad 改造 | 0.5d |
| B-01.3 | 实现跨分类翻页逻辑（next/prev + CROSSING + BOUNDARY） | `reading-engine.js` | 1d |
| B-01.4 | 进度指示升级（"3 / 20 · 科技"） | `detail.wxml` + `detail.js` | 0.5d |
| B-01.5 | 预取窗口 ±2 条 getNewsDetail | `reading-engine.js` | 0.5d |
| B-01.6 | 返回定位 `_detailReturnState` 扩展 | `detail.js` goBack | 0.5d |
| B-01.7 | 弱网兜底 + 边界 Toast | `detail.js` | 0.5d |

**依赖**：B-06 localCache.js ✅ | **验收**：AC-RQ01（跨分类衔接 + 边界提示 + 不阻断 tap）

---

### B-02 内容安全审核接入 🟡 中

| 子任务 | 描述 | 文件 | 预估 |
|--------|------|------|:---:|
| B-02.1 | 创建 `securityCheck.js`（msgSecCheck + imgSecCheck） | `cloudfunctions/common/securityCheck.js` | 1d |
| B-02.2 | refreshNews 串行接入 securityCheck | `cloudfunctions/refreshNews/index.js` | 0.5d |
| B-02.3 | sync_logs 扩展 securityCheck 字段 | `refreshNews/index.js` | 0.25d |
| B-02.4 | 告警逻辑（命中率 > 20% / 连续 3 次不可用） | `securityCheck.js` | 0.25d |

**依赖**：无（独立云函数模块） | **验收**：AC-RQ10（命中拦截 + API 不可用保守策略 + 日志）

---

### B-03 字体面板组件 🟢 小

| 子任务 | 描述 | 文件 | 预估 |
|--------|------|------|:---:|
| B-03.1 | 创建 `font-panel` 底部半屏组件 | `components/font-panel/` | 0.5d |
| B-03.2 | `theme.json` 扩展 `--font-scale` 变量（4 档） | `theme.json` | 0.25d |
| B-03.3 | 全局样式引用（detail/home/侧边栏 wxss 改用 calc） | `*.wxss` | 0.25d |
| B-03.4 | 首入跟随系统 + 记忆 | `app.js` + `font-panel.js` | 0.25d |

**依赖**：无 | **验收**：AC-RQ04（全局生效 + 记忆 + 首入跟随系统 + 无闪烁）

---

### B-04 收藏功能 🟡 中

| 子任务 | 描述 | 文件 | 预估 |
|--------|------|------|:---:|
| B-04.1 | 创建 `favorite-btn` 组件（🤍↔❤️ 状态机 + heartBeat） | `components/favorite-btn/` | 0.5d |
| B-04.2 | 底部操作栏布局（毛玻璃 + 收藏左/分享右） | `detail.wxml` + `detail.wxss` | 0.5d |
| B-04.3 | 侧边栏「❤️ 收藏」Tab + 收藏列表渲染 | `home.wxml` + `home.js` | 0.5d |
| B-04.4 | 收藏容量上限 200 条 + 满提示 | `favorite-btn.js` | 0.25d |

**依赖**：B-06 localCache.js ✅ | **验收**：AC-RQ03（收藏/取消 + 本地持久化 + 弱网可读）

---

### B-05 分享 + Canvas 占位图 🟢 小

| 子任务 | 描述 | 文件 | 预估 |
|--------|------|------|:---:|
| B-05.1 | 详情页 `onShareAppMessage` 实现 | `detail.js` | 0.25d |
| B-05.2 | 创建 `share-card` Canvas 占位图生成 | `components/share-card/` | 0.5d |
| B-05.3 | 8 分类主题色占位图模板 | `share-card.js` | 0.25d |

**依赖**：无 | **验收**：AC-RQ07（转发卡片标题+摘要+封面 + path 可回看）

---

### B-06 统一本地存储封装 🟢 小

| 子任务 | 描述 | 文件 | 预估 |
|--------|------|------|:---:|
| B-06.1 | 创建 `localCache.js`（get/set/remove/TTL/LRU） | `utils/localCache.js` | 0.5d |

**依赖**：无 | **验收**：单元测试覆盖 TTL 过期 + LRU 淘汰 + 读写一致性

---

### B-07 返回定位兼容 🟢 小

| 子任务 | 描述 | 文件 | 预估 |
|--------|------|------|:---:|
| B-07.1 | `_detailReturnState` 扩展为 `{ category, categoryIndex, newsId }` | `detail.js` | 0.25d |
| B-07.2 | 首页返回定位兼容跨分类索引 | `home.js` | 0.25d |

**依赖**：B-01（跨分类 mergedList 就绪后联调） | **验收**：详情返回首页定位到正确的分类位置

---

## 三、依赖关系图

```
B-06 (localCache) ──┬──→ B-01 (跨分类引擎)
                    │        │
                    │        └──→ B-07 (返回定位)
                    │
                    └──→ B-04 (收藏功能)

B-02 (安全审核) ──── 独立，可先行

B-03 (字体面板) ──── 独立，可先行

B-05 (分享Canvas) ── 独立，可先行
```

**建议执行顺序**：
1. **Day 1–2**：B-06 + B-02（后端先行，无前端依赖）
2. **Day 2–5**：B-01（前端核心，依赖 B-06）
3. **Day 3–4**：B-03 + B-05（前端独立组件，可与 B-01 并行）
4. **Day 5–6**：B-04（收藏，依赖 B-06）
5. **Day 6–7**：B-07（返回定位，依赖 B-01 联调）
6. **Day 7–8**：联调 + 自测 + 修复

---

## 四、待 PM 确认

| # | 确认项 | 选项 |
|---|--------|------|
| PM-1 | B-01~B-07 负责人分配 | 前端开发 / 后端开发 |
| PM-2 | 排期（起止日期） | — |
| PM-3 | 是否 2 人并行（前后端分离） | 推荐并行，预估 5–7d |
| PM-4 | 阶段四里程碑（代码冻结 / 提测日期） | — |
| PM-5 | B-01 跨分类引擎是否需要单独技术评审 | 建议 T-04 已覆盖，不需要 |

---

## 五、阶段五测试任务预告

> 阶段四完成后，以下测试任务待测试工程师执行（T-04 评审已确认）：

| ID | 任务 | 覆盖 |
|----|------|------|
| Q-01 | 跨分类阅读功能测试 | AC-RQ01（12+ 用例） |
| Q-02 | 内容安全审核测试 | AC-RQ10（8+ 用例，需注入测试内容） |
| Q-03 | 字体调节测试 | AC-RQ04（8+ 用例） |
| Q-04 | 收藏功能测试 | AC-RQ03（10+ 用例） |
| Q-05 | 分享功能测试 | AC-RQ07（6+ 用例） |
| Q-06 | 回归测试 | 现有 178 用例 + 新增 ~50 用例 |

---

> **下一步**：PM 认领 T-05，分配 B-01~B-07 负责人 + 排期 → 阶段四正式启动
> **维护**：技术负责人（任务拆解） + 项目经理（排期/资源） | **版本**：v1.0（2026-07-30，T-05 TL 侧交付）
