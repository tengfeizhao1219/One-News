# 下迭代需求规划（2026-08-03 PM 草案）

> **负责**：产品经理 | **日期**：2026-08-03
> **背景**：本期（阶段二）功能已全部交付并进入 Q-05 验收尾声（剩余 P0-Q2 翻页复测 + TL-B8 兜底实现）。下迭代需求需提前规划，明确范围与优先级，供 owner 拍板排期。
> **关联**：需求池（backlog）、TASK_BOARD「下迭代需求跟踪」、PD-B4 裁定（S6 暂缓下迭代）、Q-05 验收遗留（P1-Q5 等）。

---

## 一、下迭代候选需求清单

| 编号 | 需求/事项 | 来源 | 优先级建议 | 依赖 | 备注 |
|------|----------|------|:--:|------|------|
| N-01 | **RQ-15 侧边分类滚轮**（已启动） | 用户实测反馈 | **P0** | RQ-15-UI → RQ-15-T → RQ-15-FE | RQ-15-D ✅ 已交付；本轮主功能之一 |
| N-02 | **S6 合并「全部/推荐」分类**（8→6 后再合并 →5） | PD-B4 裁定暂缓 | **P1** | 无（本期 tab 已定型 6 分类） | 避免二次重构故留待下迭代；减少一个 Tab |
| N-03 | **BUG-20260802-005 收尾**（卡片右下角 3D 浮动设置按钮） | 用户直报 | **P1** | BLK-09 真机验证 → 移除顶栏 ⚙ | FE 已实现，验证闭环后启用 |
| N-04 | **RQ-11 个性化推荐**（轻推荐，防信息茧房） | 用户画像/竞品 | **P2** | 需数据/算法评估 | 破茧房前提：保留分类自主切换 |
| N-05 | **RQ-05 离线/弱网缓存阅读**（概要转实现） | 用户画像 | **P2** | 复用 localCache + TL-B8 兜底基建 | TL-B8 兜底（本地上次缓存）为其打底 |
| N-06 | **RQ-06 阅读历史** | 用户画像 | **P2** | 与 RQ-03 共享存储层 | 「接着看」场景 |
| N-07 | **P1-Q5 imgSecCheck 图片安全启用**（验收遗留） | Q-05 验收 | **P1** | 微信图片安全 API 接入 | 合规补强，本期未启用 |
| N-08 | **RQ-12 推送通知** | 竞品 | **P3** | 订阅消息能力评估 | 后续迭代候选 |
| N-09 | **搜索能力**（RQ-13 已砍） | — | — | — | ❌ 已砍，不进入任何迭代（owner 拍板） |

---

## 二、优先级与范围建议

### 下迭代推荐范围（方案 A · 聚焦主功能）
> **N-01（RQ-15 滚轮）P0 + N-02（合并分类）P1 + N-03（浮动按钮收尾）P1 + N-07（imgSecCheck）P1**

- 理由：RQ-15 是已启动的既定主功能，须优先完成闭环；S6 合并分类与 N-03 为已裁定/已实现项的收尾，成本低、收益明确；imgSecCheck 补合规短板。
- 预估体量：RQ-15（交互 ✅ → UI/T/FE 四环节）+ 3 个小改项。

### 可选扩展（方案 B · 含体验增强）
> 方案 A + **N-05（离线缓存转实现）P2**

- 理由：TL-B8 兜底（本地上次缓存）与离线缓存技术同源，顺势落地成本可控，直接兑现「弱网可读」用户价值。
- 代价：与 RQ-15 并行，测试面扩大。

### 后续迭代（不排入下迭代）
> N-04 个性化推荐、N-06 阅读历史、N-08 推送 —— 均需先做方案评估或依赖建设，建议列入 backlog 观察。

---

## 三、需 owner 拍板项

| # | 问题 | 建议 | 阻塞影响 |
|---|------|------|---------|
| Q-N1 | 下迭代范围选 **方案 A** 还是 **方案 B**？ | 建议 A（聚焦），B 待 TL-B8 兜底落地后评估再定 | 决定下迭代排期 |
| Q-N2 | N-07 imgSecCheck（图片安全检测）是否下迭代启用？ | 建议启用（合规补强，成本可控） | 合规风险窗口期 |
| Q-N3 | N-04 个性化推荐是否进入需求池 P2 观察？ | 建议保留候选，不做排期承诺 | 仅影响 backlog |

---

## 四、与本期验收的衔接

- 本期剩余：**P0-Q2 翻页复测（FE-C1）** + **P0-Q3 兜底实现（TL-B8）**，完成后 Q-05 升级为「通过」。
- TL-B8 兜底实现将直接服务下迭代 N-05（离线缓存）——建议 TL 定方案时预留本地缓存复用接口。
- RQ-15 各环节（UI/T/FE）可在本期验收并行推进（互不阻塞）。

---

> **下一步**：owner 拍板 Q-N1~Q-N3 后，PM 将下迭代范围固化到需求池 + 任务板，启动下迭代任务链。
