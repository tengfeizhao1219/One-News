# 开发任务单 · 侧边栏新增「全部」分类（RQ-20）

> **需求来源**：用户直连 2026-08-07
> **负责人**：产品经理（整理）｜**状态**：📋 待指派
> **基线**：main @ `291c595`（DG-03 后，CATEGORIES 无 all）

---

## 一、需求描述

在侧边新闻列表的分类中**新增「全部」分类**，放置于「推荐」**上方**。打开侧边列表**默认展示「推荐」分类**。

### 用户已确认决策

| 决策项 | 结论 |
|------|------|
| 「全部」数据范围 | **含推荐**（推荐+科技+国际+科学探索+社会 全部分类聚合） |
| 打开侧边栏默认展示 | **首次默认「推荐」+ 之后记住上次选择** |
| 首页首次加载默认分类 | **保持「推荐」**（不变） |

---

## 二、现状分析（代码级核对）

| 文件 | 现状 | 影响 |
|------|------|------|
| `utils/constants.js` | CATEGORIES = `[recommend, tech, international, sports, life]`，**无 all**（DG-03 移除） | 需首位插入 all |
| `pages/home/home.js` | `PANEL_CATEGORIES = CATEGORIES`（自动含新增分类）；`currentCategory: 'recommend'`、`panelCategory: 'recommend'`（首次默认✅）；`loadCategory()` / `onCategoryChange()` / `onWheelChange()` 通用分类切换；`_syncPanelList()` 对 `recommend`/`all` **不过滤**（L249 已有逻辑） | 前端主体已兼容，仅需确认 all 路径 |
| `cloudfunctions/getNewsList/index.js` | **all 分支已保留**（L26-28：`category === 'all'` 时不过滤分类，L39-46 stale 兜底同样支持） | 后端**无需代码改动**，仅需云端验证 |
| `pages/detail/reading-engine.js` | `READING_CATEGORIES` 已有排除 all 逻辑（L20 `id !== 'all'`）；从「全部」进入详情用透传列表构建 merged，不补拉 | 无需改动 |

> **结论**：该需求核心是**前端 1 处常量改动 + 全链路验证**；后端 all 分支已存在（旧分享链接兼容保留），无需新增。

---

## 三、任务拆分

### 🔴 FE-01 · 前端：constants.js 首位新增「全部」（依赖无）

**文件**：`utils/constants.js`

**改动**：
```js
const CATEGORIES = [
  { id: 'all', name: '全部' },        // 新增，置顶
  { id: 'recommend', name: '推荐' },
  { id: 'tech', name: '科技' },
  { id: 'international', name: '国际' },
  { id: 'sports', name: '科学探索' },
  { id: 'life', name: '社会' },
]
```

**注意**：
- `CATEGORY_MAP` 由 CATEGORIES 自动生成，无需手动加
- **首页默认分类 `currentCategory: 'recommend'` 保持不变**（不要改成 all）
- **侧边栏 `panelCategory: 'recommend'` 保持不变**（首次默认推荐✅）

### 🔴 FE-02 · 前端：验证分类切换与侧边栏行为（依赖 FE-01）

**文件**：`pages/home/home.js` + `components/category-wheel/` + `pages/home/home.wxml`

**验证/可能微调**：
1. `loadCategory('all')` → 请求 `getNewsList({category:'all'})` → 返回全部新闻，首页正常渲染
2. `_syncPanelList()` 对 `all` 不过滤（L249 已有 `cat === 'all'` 判断）✅ 确认
3. 侧边栏 `category-wheel` `activeId="{{panelCategory}}"`：打开面板高亮「推荐」✅（panelCategory 默认 recommend）
4. **记住上次**：panelCategory 是 data 持久状态，打开面板不重置 → 已满足「记住上次」✅ 确认
5. 从「全部」进入详情页：`detailContext = { category:'all', list }`，reading-engine 用透传列表、不跨分类补拉 ✅ 确认
6. 顶部分类提示（`_showCategoryHint('all')`）：需确认「全部」分类名正确显示

### 🔴 FS-01 · 后端：验证 all 分支（依赖无）

**文件**：`cloudfunctions/getNewsList/index.js`（预计**零改动**）

**验证**：
1. `getNewsList({category:'all', pageNum:1, pageSize:10})` 返回全部分类新闻（含 recommend/tech/international/sports/life）
2. all 时 `meta.source` / `stale` 逻辑正常（fresh 空才 stale 兜底）
3. 确认无需改 `CATEGORY_NAMES`（all 分支返回条目各自带 categoryName，all 本身无需名称映射）

### 🟡 QA-01 · 回归验证（依赖 FE-01/FE-02/FS-01）

**范围**：
- 侧边栏：显示「全部」置顶 +「推荐」其次；首次打开高亮推荐；切换后再打开记住上次
- 首页：默认推荐加载；点「全部」→ 展示聚合全部新闻；点其他分类正常
- 详情页：从「全部」进入可阅读、返回定位正常
- 回归 v7/v10/v11/v12/v13（CATEGORIES 变更影响面）

---

## 四、验收标准

1. **侧边栏分类顺序**：全部 → 推荐 → 科技 → 国际 → 科学探索 → 社会
2. **首次打开侧边栏**：高亮「推荐」（不是「全部」）
3. **再次打开侧边栏**：记住用户上次选中的分类
4. **首页首次加载**：仍默认「推荐」分类（非全部）
5. **点击「全部」**：首页展示全部分类新闻（含推荐），侧边栏同步
6. **从「全部」进详情**：正常阅读、返回定位正确
7. **回归**：v7/v10/v11/v12/v13 全绿

---

## 五、风险与备注

- ⚠️ **这是对 DG-03 决策的增量调整**：DG-03 曾「去除 all，推荐独立」。本次新增 all 但**推荐仍独立保留**（首页默认+AI精选），all 作为聚合视图补充——与 DG-03 不冲突，但需在 TASK_BOARD 标注避免误解。
- ⚠️ **「全部」可能数据量较大**：全部分类聚合，首屏 10 条 + 翻底 5×3，注意性能（预计无压力，news_cache 总量有限）。
- ⚠️ **后端 all 分支虽已存在**，但自 DG-03 后前端已不再请求 all，需云端实测确认返回数据正常（FS-01）。

---

## 六、任务分配表

| 任务 | 负责人 | 优先级 | 依赖 | 预估 |
|------|:---:|:---:|------|:---:|
| FE-01 constants.js 新增 all | 前端（FE） | 🔴 P0 | 无 | 5min |
| FE-02 分类切换/侧边栏验证 | 前端（FE） | 🔴 P0 | FE-01 | 0.5h |
| FS-01 getNewsList all 验证 | 全栈（FS） | 🔴 P0 | 无 | 0.5h |
| QA-01 回归验证 | 产品经理（PM） | 🟡 P1 | FE-01/02 + FS-01 | 1h |

---

> **PM** ｜ 2026-08-07
> 下一步：写入 TASK_BOARD 广播 + 指派 FE/FS → 开发 → PM 按 §四 验收
