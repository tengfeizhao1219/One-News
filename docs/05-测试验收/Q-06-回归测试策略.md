# Q-06 回归测试策略

> **任务 ID**：Q-06 | **优先级**：🔴 P0 | **覆盖**：全部 AC（阶段二 + 基线）
> **测试对象**：178 用例基线 + Q-01~Q-05 新增用例 + 全链路
> **参考文档**：A-12d 测试策略预审 | 测试策略总纲 | 各版本回归报告
> **日期**：2026-07-31 | **测试工程师**：QA

---

## 一、回归范围

### 1.1 自动化回归（必须全量通过）

| 脚本 | 用例数 | 覆盖 | 触发条件 |
|------|:------:|------|----------|
| `test/v4-regression-data-layer.js` | 180 | L1 天行→L2 内存→L3 云库→L4 聚合→L5 AI | 阶段二任何代码合入 |
| `test/v4-regression-integration.js` | 173 | 集成链路 | 同上 |
| `test/v4-regression-validator.js` | 67 | 数据校验器 | 同上 |
| `test/v4-regression-llmsearch.js` | 55 | LLM 搜索 | 同上 |
| `test/v5-regression-touch-architecture.js` | 7 | 手势 WXS 架构 | WXS 改动时 |
| `test/v6-regression-bug1-bug2.js` | 25 | Bug1/2 修复验证 | 每次合入 |
| `test/v7-regression-reading-mode.js` | 37 | 阅读模式静态检查 | B-01 相关改动 |
| **合计** | **544** | | |

### 1.2 新增功能回归（Q-01~Q-05 用例汇总）

| 来源 | 用例数 | P0 | P1 | P2 |
|------|:------:|:--:|:--:|:--:|
| Q-01 跨分类阅读 | 18 | 8 | 10 | — |
| Q-02 字体面板 | 8 | 4 | 4 | — |
| Q-03 收藏功能 | 10 | 5 | 5 | — |
| Q-04 分享功能 | 7 | 5 | 2 | — |
| Q-05 返回定位 | 6 | 4 | 2 | — |
| **合计** | **49** | **26** | **23** | **0** |

### 1.3 全链路冒烟（手动，每轮回归必跑）

| 场景 | 路径 |
|------|------|
| 首页 → 详情 → 翻页（跨分类）→ 返回 | 核心阅读闭环 |
| 首页 → 详情 → 收藏 → 返回 → 侧边栏收藏 Tab → 回看 | 收藏闭环 |
| 首页 → ⚙️ 字体面板 → 调字号 → 进详情验证 | 字体闭环 |
| 首页 → 详情 → 分享 → 从分享链接进入 | 分享闭环 |
| 暗色模式切换 → 遍历上述路径 | 暗色兼容 |

---

## 二、执行策略

### 阶段 1：自动化回归（开发者工具）

```bash
node test/v4-regression-data-layer.js
node test/v4-regression-integration.js
node test/v4-regression-validator.js
node test/v4-regression-llmsearch.js
node test/v5-regression-touch-architecture.js
node test/v6-regression-bug1-bug2.js
node test/v7-regression-reading-mode.js
```

**通过标准**：544/544 全部通过，0 失败。

### 阶段 2：功能回归（开发者工具 + 真机）

按 Q-01→Q-05 顺序逐份执行，记录结果。

**通过标准**：P0 26/26 全部通过；P1 23/23 ≥90%。

### 阶段 3：全链路冒烟（真机）

按 1.3 的 5 条路径逐条验证。

**通过标准**：5/5 全部通过。

---

## 三、回归门禁

| 门禁 | 条件 | 不满足时 |
|------|------|----------|
| 自动化回归 | 544/544 通过 | 🚫 阻断，修复后重跑 |
| 功能 P0 | 26/26 通过 | 🚫 阻断发布 |
| 功能 P1 | ≥90% | ⚠️ 记录遗留 |
| 全链路冒烟 | 5/5 通过 | 🚫 阻断发布 |

---

## 四、新增建议

> 基于 A-12d 预审结论，建议新增以下回归脚本（可在阶段五完成后补）：

| 脚本 | 覆盖 | 优先级 |
|------|------|:------:|
| `test/v8-regression-cross-category.js` | AC-RQ01 跨分类数据逻辑 | 🔴 |
| `test/v9-regression-content-safety.js` | AC-RQ10 合规 API 调用/错误 | 🔴 |
| `test/v10-regression-favorites.js` | AC-RQ03 收藏存储 CRUD | 🟡 |

---

> **编写人**：测试工程师 | **日期**：2026-07-31
