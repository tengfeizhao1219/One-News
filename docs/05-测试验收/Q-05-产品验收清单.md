# Q-05 产品验收清单

> **任务**：Q-05（阶段五产品验收） | **负责**：产品经理 | **日期**：2026-07-31
> **输入**：PRD §7 验收标准 + D-04 设计评审纪要 + D-02 交互定案 + D-03 视觉定案
> **状态**：待测试工程师完成 Q-01~Q-04 后启动

---

## 验收总览

| 功能 | 编号 | 优先级 | 状态 |
|------|:--:|:--:|:--:|
| 跨分类连续阅读 | F-01 | P0 | 待验收 |
| 内容安全入库校验 | F-02 | P0 | 待验收 |
| 字体大小调节 | F-03 | P1 | 待验收 |
| 收藏 | F-04 | P1 | 待验收 |
| 分享 | F-05 | P1 | 待验收 |
| 离线缓存 | F-06 | P2 | 本期不验收 |
| 阅读历史 | F-07 | P2 | 本期不验收 |

---

## F-01 跨分类连续阅读（P0 🔴）

> PRD §5.1 | 方案 A：全分类预合并列表 | 交互定案 D-02-1

### 正向流程

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 1.1 | 同分类翻页不受影响 | 详情页上滑翻页（同一分类内） | 同分类上下滑正常工作，"3 / 20" 正常递增/递减 |
| 1.2 | 跨分类衔接 | 滑到本分类**最后一条**，继续上滑 | 自动展示**下一分类首条**，无需返回首页 |
| 1.3 | 进度指示升级 | 跨分类切换时 | `positionText` 从 "3 / 20" → "1 / 15 · 科技"（追加分类名 + 分类强调色） |
| 1.4 | 连续跨多分类 | 连续上滑跨 2+ 个分类 | 每个分类边界平滑过渡，分类名正确切换 |
| 1.5 | 分类色闪烁提示 | 跨分类切换瞬间 | 导航栏下方出现分类色闪烁条（宽 40rpx，200ms 淡入淡出） |

### 边界场景

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 1.6 | 全部分类最后一条 | 滑到最后一个分类的末条，继续上滑 | 底部 Toast "已经是最后一条" + "← 返回首页" 可点击（2s 消失） |
| 1.7 | 全部分类第一条 | 滑到第一个分类的首条，继续下滑 | "已经是第一条 · 上滑返回"（复用现有逻辑） |
| 1.8 | 某分类仅 1 条 | 该分类仅 1 条新闻，滑到最后 | 上滑即切换到下一分类首条（不卡在 1/1） |
| 1.9 | 8 分类全部为空 | 所有分类均无数据 | 详情页不崩溃，展示空态提示 |

### 子元素 tap 不阻断

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 1.10 | 正文滚动不阻塞 tap | 详情页正文区滚动时点击底部操作栏 | 收藏/分享按钮正常响应（WXS 无 `return false`） |
| 1.11 | 跨分类翻页不影响 tap | 跨分类衔接动画过程中点击返回 | 正常返回首页 |

### 网络/弱网兜底

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 1.12 | 断网 + 目标分类无缓存 | 断网状态滑到跨分类边界 | 停在当前内容，底部 Toast "网络开小差，下拉重试"（黄色，3s） |
| 1.13 | 下拉重试 | 断网后恢复网络，下拉 | 重新触发跨分类加载，正常衔接 |

### 返回定位兼容

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 1.14 | 跨分类后返回 | 跨 2 个分类后点返回 | 首页侧边栏定位到最终停留的分类，高亮对应条目 |
| 1.15 | 同分类返回不变 | 仅同分类翻页后返回 | 返回行为不变（现有逻辑正常） |

### 动画一致性

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 1.16 | 翻页动画 | 跨分类翻页 | 复用 `out-up/in-up`（350ms），与同分类一致 |

---

## F-02 内容安全入库校验（P0 🔴）

> PRD §5.2 | CloudBase `cloud.openapi.security.msgSecCheck` | 保守策略兜底

### 合规命中

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 2.1 | 文本违规拦截 | refreshNews 拉取含违规词新闻 | 该条**不入 news_cache**，`sync_logs` 记录 `securityCheck={passed:false, hitType, hitReason, checkedAt}` |
| 2.2 | 图片违规拦截 | 含违规图片的新闻 | 图片经 `imgSecCheck` 检测，违规则整条不入库 |
| 2.3 | 混合批量 | 一批 50 条中 5 条违规 | 违规 5 条拦截，其余 45 条正常入库 |

### API 异常兜底

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 2.4 | API 不可用 | 模拟 msgSecCheck 超时/错误 | **该批暂缓入库**（不绕过校验），告警日志 ERROR 级 |
| 2.5 | 保守策略不绕过 | API 连续不可用 | 不发生「跳过校验直接入库」的情况 |
| 2.6 | 告警触发 | 单次命中率 > 20% | 云函数日志 ERROR 级记录 |

### 与 validator 分工

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 2.7 | validator 仍生效 | 查看 refreshNews 流程 | `validator`（字段/来源/垃圾词）→ `securityCheck`（违规）→ `batchInsert`，顺序正确 |
| 2.8 | 各自独立 | 模拟 validator 通过但 security 拦截 | 最终以安全 API 拦截为准，不入库 |

---

## F-03 字体大小调节（P1 🟡）

> PRD §5.3 | `--font-scale` 4 档 | 底部半屏面板 | 首入跟随系统

### 入口与面板

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 3.1 | 入口位置 | 首页顶部栏右侧 | ⚙️ 设置图标可见，点击弹出底部半屏面板（300ms slideUp） |
| 3.2 | 面板内容 | 打开面板 | 显示 4 档单选：标准 / 大 / 特大 / 超大，当前档位高亮 |
| 3.3 | 关闭方式 | 点击遮罩 / 3s 无操作 | 面板 slideDown 250ms 消失 |
| 3.4 | 面板本身不缩放 | 面板内预览文字 | 面板内文字不受字体档位影响（防止递归放大） |

### 档位切换

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 3.5 | 即时生效 | 选择档位 | 预览文字实时缩放，无需确认，选中即保存 |
| 3.6 | 无闪烁 | 切换档位、页面间跳转 | 字号平滑过渡 `transition: font-size 150ms ease`，无白屏 |
| 3.7 | 全局生效 | 切换档位后查看首页卡片、侧边栏、详情页 | 所有文字统一缩放 |

### 记忆与系统跟随

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 3.8 | 记忆 | 设置超大档后关闭小程序，重新打开 | 字号保持超大，不从标准开始 |
| 3.9 | 首入跟随系统 | 首次启动（无 storage 记录） | 根据系统字号映射到对应档位（标准/大/特大/超大） |
| 3.10 | 手动调整后不跟随 | 用户手动调过 → 改系统字号 → 重新打开 | 保持用户手动设置，不重新跟随系统 |

### 固定元素不缩放

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 3.11 | 图标/热区固定 | 超大字号下 | 导航栏高度、操作栏高度、图标尺寸、点击热区 ≥ 88rpx 不变 |
| 3.12 | 按钮文字缩放 | 超大字号下 | 底部操作栏按钮文字跟随缩放 |

### 超大字号截断保护

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 3.13 | 标题截断 | 超大字号（×1.5）下查看首页卡片 | 标题限 3 行，超出截断 |
| 3.14 | 摘要截断 | 同上 | 摘要限 6 行 |
| 3.15 | 元信息不换行 | 同上 | 元信息 `white-space: nowrap` |

---

## F-04 收藏功能（P1 🟡）

> PRD §5.4 | 本地优先 `wx.setStorageSync` | 200 条上限 | 侧边栏「❤️ 收藏」Tab

### 收藏操作

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 4.1 | 收藏按钮 | 详情页底部操作栏左侧 | 🤍 空心灰色按钮可见 |
| 4.2 | 点击收藏 | 点击 🤍 | 按钮态切换为 ❤️ 实心红色（#FF3B30），heartBeat 300ms 动画 |
| 4.3 | 取消收藏 | 点击 ❤️ | 按钮态切回 🤍，数据从 storage 移除 |
| 4.4 | 重复收藏幂等 | 快速双击 🤍 | 不会产生重复记录 |

### 收藏列表

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 4.5 | 入口 | 首页侧边栏 | 分类 Tab 最右侧显示「❤️ 收藏」Tab |
| 4.6 | 列表展示 | 点击收藏 Tab | 收藏项按 `addedAt` 倒序排列，复用 `panel-item` 样式 |
| 4.7 | 点击回看 | 点击列表项 | `navigateTo detail`，支持跨分类阅读 |
| 4.8 | 空态 | 无收藏时 | 显示「还没有收藏任何新闻」+ 引导文字 |

### 本地持久化

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 4.9 | 持久化 | 收藏后关闭小程序，重新打开 | 收藏数据不丢 |
| 4.10 | 弱网可读 | 断网状态打开收藏列表 | 正常展示（本地存储不依赖网络） |
| 4.11 | 容量上限 | 收藏到 200 条后再收藏 | Toast "收藏已满，请清理后重试"，不崩溃 |

### 暗色模式

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 4.12 | 已收藏色 | 暗色模式 | ❤️ 实心红色 #FF453A |

---

## F-05 分享功能（P1 🟡）

> PRD §5.5 | `onShareAppMessage` | Canvas 占位图兜底 | 标题截断 30 字

### 分享入口

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 5.1 | 分享按钮 | 详情页底部操作栏右侧 | ↗️ 分享按钮可见 |
| 5.2 | 调起转发 | 点击 ↗️ | 微信原生转发面板弹出（`open-type="share"`） |

### 转发卡片

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 5.3 | 标题截断 | 长标题新闻分享 | 标题 ≤ 30 字 + 省略号 |
| 5.4 | 摘要 | 分享卡片 | 含一句摘要 |
| 5.5 | 有图封面 | 含 picUrl 新闻分享 | 封面图为实际图片 |
| 5.6 | 无图兜底 | 无 picUrl 新闻分享 | Canvas 生成 400×300 分类主题色占位图（标题 + 分类标签） |
| 5.7 | path 回看 | 接收方点击转发卡片 | 直达该新闻详情，位置正确 |

### 边界

| # | 验收项 | 操作 | 期望 |
|:--:|------|------|------|
| 5.8 | 分享取消 | 转发面板点击取消 | 回到详情页，无异常 |
| 5.9 | Canvas 性能 | 无图分享首次触发 | 占位图生成延迟 < 200ms，不阻塞 UI |

---

## F-06/F-07 P2 概要（本期不验收）

| 功能 | 说明 |
|------|------|
| F-06 离线缓存 | P2，本期仅出概要设计。验收延至下迭代 |
| F-07 阅读历史 | P2，同上 |

---

## 埋点验收（产品视角）

> PRD §6 | 以下不需要产品逐条操作，确认埋点已上线即可

| 指标 | 口径 | 验收方式 |
|------|------|:--:|
| 跨分类衔接触发率 | 详情页触发跨分类翻页次数 / 详情页 PV | 确认埋点已部署 |
| 字号调节使用率 | 调整过字体的用户 / DAU | 确认埋点已部署 |
| 收藏渗透率 | 收藏过至少一次的用户 / DAU | 确认埋点已部署 |
| 分享次数 / 回流 | 分享卡片点击数 / 回流详情 PV | 确认埋点已部署 |
| 合规拦截率 | 被安全 API 拦截条数 / 入库总数 | 确认服务端日志 |
| 合规漏放 | 用户举报命中数 | 确认监控通道 |

---

## 不本期功能（确认未误入）

| 功能 | 预期 | 验收 |
|------|------|:--:|
| 搜索页 | 砍掉（A-10） | 确认首页无搜索入口、app.json 无 search 页面注册 |
| 独立分类 tab | 不新增（A-09） | 确认仍 8 分类 tab |
| 分享到朋友圈 | P2 | 确认本期无此入口 |
| 个性化推荐/推送 | 后续迭代 | 确认无相关代码/入口 |

---

## 验收结论模板

```
验收人：产品经理
验收日期：___年__月__日
版本：阶段二 v1.0
测试报告：Q-02 功能测试报告（待测试工程师交付）

按上方清单逐项验收，每项标记 ✅/❌/⚠️：
- ✅ 通过
- ❌ 不通过（需创建 Bug 事项，附复现步骤+期望+实际）
- ⚠️ 有条件通过（记录条件，不阻塞上线但需跟进）

阻断上线判定：
- 任一 P0 项 ❌ → 阻断上线
- P1 项 ❌ ≥ 3 个 → 建议阻断，与项目经理协商

结论：___（通过/有条件通过/不通过）
遗留问题：___
```

---

> **下一步**：等测试工程师完成 Q-01~Q-04（功能测试报告）后，按本清单逐条验收。
