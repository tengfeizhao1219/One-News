# Mock 回归测试报告

> 🗂️ **已归档（2026-08-03）**：owner 裁定删除 mock 数据（`data/news.json`、`cloud-import-data.json` 已物理删除），全链路使用真实数据；本报告记录历史 Mock 模式测试结果，仅存档参考，不再作为现行验收依据。
>
> 测试日期：2026-07-28  
> 测试人：QA  
> 变更范围：实时新闻获取功能（云函数改造 + 前端骨架屏/状态页 + 错误处理）  
> 测试模式：USE_MOCK = true + 模拟器注入异常场景

---

## 一、测试环境

| 项目 | 值 |
|------|-----|
| 测试框架 | Node.js 模拟器（test/mock-regression.js） |
| Mock 数据 | mock/news.js（18 条，5 个分类） |
| 模拟器 | mock/simulator.js（支持 normal/error/empty/slow 四种场景） |

---

## 二、测试结果

### 场景1：normal — 正常加载

| # | 测试项 | 结果 | 备注 |
|---|--------|------|------|
| 1 | 首页加载 — 返回列表非空 | ✅ 通过 | 返回 10 条数据 |
| 2 | 列表项包含必要字段（id/title/summary/category 等） | ✅ 通过 | formatNewsItem 输出格式正确 |
| 3 | 分类筛选 — tech | ✅ 通过 | 4 条科技新闻 |
| 4 | 分类筛选 — sports | ✅ 通过 | 3 条体育新闻 |
| 5 | 分页 — hasMore 正确 | ✅ 通过 | pageSize=5, total=18, hasMore=true |
| 6 | 分页 — 大页码返回空 | ✅ 通过 | page=100 返回 [] |

### 场景2：normal — 搜索功能

| # | 测试项 | 结果 | 备注 |
|---|--------|------|------|
| 7 | 关键词搜索 — 匹配结果 | ✅ 通过 | 搜索"AI"返回 3 条 |
| 8 | 空关键词搜索 | ✅ 通过 | 返回空数组 |
| 9 | 无匹配关键词搜索 | ✅ 通过 | 返回 total=0 |

### 场景3：error — 网络错误

| # | 测试项 | 结果 | 备注 |
|---|--------|------|------|
| 10 | getNewsList 抛出异常 | ✅ 通过 | errorCode = SIMULATED_ERROR |
| 11 | searchNews 抛出异常 | ✅ 通过 | 异常正确传递 |
| 12 | handleApiError 友好提示 | ✅ 通过 | 文案正确映射 |

### 场景4：empty — 空数据

| # | 测试项 | 结果 | 备注 |
|---|--------|------|------|
| 13 | getNewsList 返回空列表 | ✅ 通过 | list=[], total=0, hasMore=false |
| 14 | searchNews 返回空列表 | ✅ 通过 | list=[], total=0 |

### 场景5：slow — 慢网络

| # | 测试项 | 结果 | 备注 |
|---|--------|------|------|
| 15 | 延迟后正常返回 | ✅ 通过 | 延迟 5s 后数据正确返回 |

---

## 三、统计

| 指标 | 值 |
|------|-----|
| 总用例数 | 15 |
| 通过 | 15 |
| 失败 | 0 |
| 阻塞 | 0 |
| 通过率 | **100%** |

---

## 四、Bug 清单

无。

---

## 五、测试发现与备注

1. **搜索关键词依赖实际数据**：测试时发现 mock 数据中不含"科技"字样（科技类新闻使用"AI""芯片"等术语），改用"AI"后测试通过。这是测试数据特征问题，非代码 Bug。
2. **模拟器工作正常**：error/empty/slow 三种异常场景均正确触发，前端 `handleApiError` 能正确映射错误码。
3. **formatNewsItem 输出格式稳定**：所有字段映射正确，与云函数返回格式一致。

---

## 六、结论

- [x] **通过** — Mock 回归测试全部通过，可进入集成测试阶段
- [ ] ~~不通过~~

---

## 七、签字

| 角色 | 结论 | 日期 |
|------|------|------|
| QA | 通过 | 2026-07-28 |
| 开发 | 确认 | 2026-07-28 |
