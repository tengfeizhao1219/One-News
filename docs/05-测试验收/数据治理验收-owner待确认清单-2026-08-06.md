# 数据治理验收 · owner 待确认 / 待执行清单

> **对象**：owner（项目 owner / 技术决策者）
> **发起**：产品经理（PM）| **日期**：2026-08-06
> **背景**：数据治理 DG-01~05 已全部交付，代码级验收（DG-06 第一层）通过。以下事项需 owner **拍板决策** 或 **真机/云端执行** 后方可推进验收结论升级为「通过」。

---

## 一、需 owner 拍板决策（P0/P1）

### 🔴 Q1 · recommend 首屏条数口径（P0）

**问题**：DG-01 中 `recommend` 抓取量已从 10 条/轮 **降至 8 条/轮**（`zhipuSearch.js` L180-184，智谱 web_search 10 条 prompt 50s 超时实证，折中 8 条/~45s 留降级余量）。

**影响**：原验收标准「首页首屏 10 条」与现状不符。DG-03 用「首屏 8 + 翻底拉取追加」适配，但**首屏实际展示可能为 8 条**（recommend 数据源每轮仅 8 条）。

**需 owner 裁定**：
- [ ] 接受「首屏 8 条 + 翻底追加至 10+」口径（推荐，零改动）
- [ ] 要求首屏严格 10 条（需调整前端首屏补拉逻辑或恢复 recommend 10 条 + 接受超时风险）
- [ ] 其他方案：________________

> 备注：此口径直接关系验收标准 ②（推荐分类）与 ⑦（拉取节奏）的判定。

---

### 🟡 Q2 · AI 摘要胶囊展示（P1）

**问题**：云端 `ZHIPU_API_KEY` 未配置，AI 摘要胶囊在小程序端不显示（FS 07:40 诊断确认）。

**需 owner 裁定**：
- [ ] 部署时配置 `ZHIPU_API_KEY`（推荐，恢复 AI 摘要能力）
- [ ] 暂不接受，AI 摘要作为后续迭代
- [ ] 改用其他模型 Key（如 DeepSeek，注意余额）

---

## 二、需 owner 真机 / 云端执行（P0）

### 🔴 Q3 · 云函数重新部署（P0）

DG-01 改动了 `getNewsList`（stale 兜底收紧）与 `refreshNews/zhipuSearch.js`（recommend 8 条），**代码已提交但未上云**。

**需 owner 执行**：
- [ ] 微信开发者工具重新上传 `getNewsList` + `refreshNews` 云函数并云端安装依赖
- [ ] 4 个已停用云函数（recordBrowse / getBrowseHistory / setUserFavorite / getUserFavorites）**无需部署**（代码保留标记 DEPRECATED）

**验证**：部署后触发 refreshNews，观察日志 recommend 分类条数=8、无超时。

---

### 🔴 Q4 · 真机走查验收（P0）

DG-06 第一层（代码级）已通过，需 owner 真机确认以下 8 项实际效果：

| # | 验收项 | 真机操作 | 预期 |
|:--:|------|------|------|
| 1 | 推荐默认 | 打开小程序 | 首页默认「推荐」分类 |
| 2 | 侧栏置顶 | 左滑呼出侧栏 | 第一项「推荐」置顶 |
| 3 | 列表一致性 | 翻底追加 → 进详情上下滑 | 末条与首页末条一致，positionText 不漂移 |
| 4 | 跨分类跳转 | 读完「推荐」末条上滑 | 自动加载「科技」+ toast「正在阅读：科技」 |
| 5 | 纯本地存储 | 收藏/浏览后断网重开 | 数据仍在；console 无云端调用报错 |
| 6 | 来源顺序 | 从历史/收藏进详情 | 仅来源列表内滑动，无跨分类；末条 toast「已阅读完…」|
| 7 | 拉取节奏 | 翻底上滑 3 次 | 第 4 次 toast「已阅读了 N 条新闻，建议稍后再读」|
| 8 | 过期兜底 | 等缓存过期后打开 | 列表不空白（backup/builtin 兜底）|

---

### 🔴 Q5 · V5-FS-02 数据清洗云端验证（P0）

V5-FS-02 的 6 项修复（正文串入其他新闻 / 国际分类限流 / 广告声明 / 图片说明 / 微信分享引导 / remove limit）**代码已交付（v6.3+）但待云端部署验证**。

**需 owner 执行**：部署后跑 TC-C01~C03（清洗噪音）+ TC-F04（国际分类），回报结果 → PM 据实判定 V5-PM-01 是否 ✅。

---

## 三、需 owner 关注（P1/P2）

### 🟡 Q6 · DeepSeek 402 余额不足

同日 2 次出现 DeepSeek 402（余额不足）。建议充值或确认降级策略（聚合兜底已保证不断供，但影响摘要质量）。

### 🟢 Q7 · 纯本地存储取舍已接受

换手机 / 清缓存 / 删小程序 → 历史+收藏全部丢失（owner 已接受，后续完善云同步）。无需动作，仅知会。

### 🟢 Q8 · 云端历史/收藏数据废弃

browse_history / favorites 集合冻结保留，不迁移。无需动作。

---

## 四、owner 执行后回报路径

| 事项 | 回报给 | 方式 |
|------|------|------|
| Q1 口径裁定 | PM | 本清单勾选回复 |
| Q3 部署完成 | PM | 广播区 / TASK_BOARD |
| Q4 真机结果 | PM | 验收报告 §二 逐项回填 |
| Q5 清洗验证 | PM | V5-PM-01 验收执行清单更新 |

> 全部 P0（Q1/Q3/Q4/Q5）闭环后，数据治理验收结论升级为「✅ 通过」，进入上线复盘阶段。
