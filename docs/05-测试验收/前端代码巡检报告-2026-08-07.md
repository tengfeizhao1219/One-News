# 前端代码巡检报告 · 2026-08-07

> **巡检人**：小程序前端开发（FE-2）
> **基线**：main @ `7deb2b2`（2026-08-07 09:48 同步）
> **范围**：pages/ · components/ · utils/ · app.js · test/（死代码 / 硬编码 / toast·console 残留 / 注释·断言漂移）
> **结论**：**无 P0 问题**；P1 死代码 5 处、P2 观察项 2 项、P3 遗留 4 项、本地残留一批（不入远端）

---

## 一、死代码（JS 定义未调用）

### 🔴 P1 · 高置信度（5 处，建议清理）

| # | 位置 | 方法 | 证据 |
|---|------|------|------|
| 1 | `pages/detail/detail.js:894` | `_getDefaultPlaceholder` | 全项目无调用；注释自认 FS-02 后等效逻辑已删，仅残留空函数 |
| 2 | `components/category-wheel/category-wheel.js:74` | `_clampIndex` | 组件内 0 次调用（onTouchMove 用内联 Math.max/min 代替） |
| 3 | `components/share-card/share-card.js:206` | `getCategoryColor` | 注释称"供外部引用"，但全项目无引用 |
| 4 | `utils/cloud.js:88` | `getPendingFavorites` | 导出后前端无调用（收藏已纯本地化，仅测试引用） |
| 5 | `pages/about/about.js:77` | `goHome`（重复定义） | 与 L105 重复定义，后者覆盖前者，L77 永不生效 |

### 🟡 P2 · 中置信度（15 处，需连带处理测试，谨慎清理）

| 类别 | 位置 | 说明 |
|------|------|------|
| 旧事件处理器（UI 已改版） | `home.js:830` `onNavDotTap`、`:948` `onCategoryChange` | wxml 无绑定（导航点 UI 已移除；侧栏改用 onPanelItemTap/onWheelChange），**仅测试文件直接调用** |
| 旧事件处理器 | `favorites.js:108` `onSyncRetry` | wxml 无绑定（云同步条已删），**仅测试断言** |
| 旧事件处理器 | `settings.js:217`、`about.js:92` `goBack` | wxml 均无绑定，注释自述"原 nav-back 已改主页按钮" |
| 未使用常量导出 | `constants.js`：`BOUNCE_ANIMATION_MS`（注释自述已废弃）、`SWIPE_ANIMATION_MS`、`PAGE_WIDTH`、`STATUS_BAR_HEIGHT`、`HISTORY_LIMIT`、`FAVORITES_LIMIT` | 导出后前端均未引用（测试可能引用） |
| 未使用工具函数 | `util.js:31,42` `debounce`、`throttle` | 导出但无调用 |
| LocalCache 部分 API | `localCache.js`：`prune`/`_evictExpired`/`has`/`clear`/`maxItems` | 生产代码未用（仅测试） |
| cloud.js 导出 | `callCloudFunction`、`enqueue` | 外部未引用（部分被测试引用） |
| 解构未使用 | `home.js:3` `SWIPE_THRESHOLD` | 解构后未使用（内部硬编码 70），**存在不一致**：常量 70 与导入值需核对 |

> ⚠️ 清理 P2 时必须同步处理 `test/` 中直接调用这些方法的测试（onNavDotTap/onCategoryChange/onSyncRetry/getPendingFavorites 等被测试引用），否则测试挂。

---

## 二、本地残留文件（远端 main 已清理 / 不存在，未入库）

| 文件 | 状态 | 说明 |
|------|------|------|
| `components/font-panel/`（4 文件） | 本地残留 | 远端 FS-04 已删（commit `c1043aa`），本地工作区未清理 |
| `assets/icons/plus.svg` | 本地残留 | 未追踪、零引用 |
| `test/v12-tlb13-favorites-sync.js` | 本地残留 | 远端 test/ 无此文件；且断言「已收藏（待同步）」toast 已被 BUG-20260806-004 静默化 → **断言过期** |
| `test/v4-regression-data-layer.js`、`v4-regression-integration.js`、`v4-regression-llmsearch.js` | 本地残留 | 远端 test/ 无此文件 |

> 建议：本地直接删除或 gitignore，不入远端。

---

## 三、硬编码残留（对照 D-08 治理基线）

| # | 位置 | 内容 | 级别 | 说明 |
|---|------|------|------|------|
| 1 | `pages/detail/detail.wxss:221-222` | 提示浮层 `background: rgba(0,0,0,0.78)` + `color: #ffffff` | 🟡 P2 | 黑底白字浮层未走 token；深浅色通用，可评估 token 化 |
| 2 | `pages/favorites/favorites.wxss:213` / `history.wxss:154` | 浅色按压态 `rgba(0,0,0,0.04)` | 🟢 P3 | **PD 已记录**（D-07 走查：浅色按压态裸色未 token 化，P3 规范优化，不阻断闭环） |
| — | box-shadow 系列 rgba / 暗色 `#1C1C1E` / switch-knob `#FFFFFF` | 豁免 | ✅ | D-08 阴影豁免 + 设计决策有注释（settings.wxss:135-142） |

---

## 四、toast / console 残留

| # | 位置 | 内容 | 级别 | 说明 |
|---|------|------|------|------|
| 1 | `pages/home/home.js:780` | `wx.showToast('已经到底啦')`（loadMoreNews 翻底） | 🟡 P2 | 跨分类切换已改状态栏胶囊（BUG-20260806-023），但 loadMoreNews 翻底"到底"提示仍用 toast，**风格不统一**，可评估是否也换胶囊 |
| 2 | console.log | 0 处残留 | ✅ | RQ-08 已闭环 |

> 其余 toast（刷新中/已加载 N 条/错误提示/已读上限）均为正常操作反馈，保留合理。

---

## 五、注释 / 断言漂移

| # | 位置 | 内容 | 级别 |
|---|------|------|------|
| 1 | `test/v10-regression-fe-c1-bugfix.js:364,373` | 断言值已按 600ms 同步，但**注释文案仍写"500ms"** | 🟢 P3 |
| 2 | BUG 编号注释（13 处 `BUG-20260806-004`） | 全部对应导航胶囊对齐 + rpx 固定，无漂移 | ✅ |
| 3 | `category-wheel.wxss` 注释 | PD 08-06 备注的 004 改 005 漂移**已修复**（现为 005/018 等正确编号） | ✅ |

---

## 六、处理建议（供 PM/团队决策）

1. **P1 死代码 5 处**：建议 FE 直接清理（约 15 分钟），不含测试依赖风险；
2. **本地残留**：建议本地删除 font-panel/plus.svg + 4 个未入库 test 文件（v12 断言已过期）；
3. **P2 两项**（detail 浮层 token 化、翻底"到底"提示换胶囊）：纳入下批小改；
4. **P2 中置信度死代码**：建议连 test 一起评估后清理（约 0.5-1h）；
5. **P3 三项**（按压态 token、v10 注释文案）：随手修。

---

## 七、已处理记录（FE-2 执行）

| 日期/时间 | 提交 | 处理项 |
|-----------|------|--------|
| 2026-08-07 09:5x | `c5b7bb5` | **P1 清理**：detail._getDefaultPlaceholder、category-wheel._clampIndex、share-card.getCategoryColor、cloud.getPendingFavorites、about 旧 goHome（L77） |
| 2026-08-07 10:09 | 本地 | **本地残留清理**：font-panel/(4 文件)、plus.svg、4 个未入库 test → 移入 /tmp 备份 |
| 2026-08-07 10:09 | 本地+`c75bbcc` | **P3 注释**：v10 测试 500ms→600ms（2 处）；**P2 清理**：home.onNavDotTap、favorites.onSyncRetry、settings/about.goBack、util.debounce/throttle、constants.SWIPE_ANIMATION_MS/BOUNCE_ANIMATION_MS；**一致性修复**：home.onTouchEnd 硬编码 70→SWIPE_THRESHOLD；**测试同步**：v10 mock 清理、v511 断言匹配 SWIPE_THRESHOLD |

**保留项（有测试/设计锚点，未删）**：
- `localCache.prune/has/clear/_evictExpired` + `maxItems`：b06 测试覆盖的正式 API
- `onCategoryChange`：v10 测试直接调用
- `constants.PAGE_WIDTH/STATUS_BAR_HEIGHT`：运行时动态配置体系环节
- `constants.HISTORY_LIMIT/FAVORITES_LIMIT`：数据治理"常量统一"设计锚点
- `cloud.callCloudFunction/enqueue`：cloud.js 内部核心逻辑

**回归**：v5 18/0 · v6 20/0 · v7 ✅ · v9 32/0 · v10 53/0 · v11 28/0 · v13 86/0 · v511 40/0 · b02 10/0 · b06 20/0 · v4 100%

---

> FE-2 ｜ 2026-08-07 ｜ 基线 main `c75bbcc`（巡检+清理全部完成）
