# 交互语言标准 v1.0 对照代码走查报告

> **日期**：2026-08-02 | **走查人**：交互设计师 | **基准**：[D-02-交互语言标准-v1.0.md](./D-02-交互语言标准-v1.0.md) §11 验收清单
> **方法**：逐项对照当前 main 分支源码（commit `3515e5a`），按标准 §1~§7 六大体系审计。
> **上下文**：已读取最新 TASK_BOARD 广播（2026-08-02 PM 巡检）、COMMLOG（分支清理/B-10单测/沟通机制）、前端最新任务池（UX-BUG09~14 + UX-IMPROVE04~07 + BUG-007~009 + UX-SIMPLIFY01/05/07）。

---

## 一、总览

| 体系 | 检查项数 | 通过 | ⚠️ 偏差 | 🔴 违规 | 通过率 |
|------|:---:|:---:|:---:|:---:|:---:|
| §1 手势语言 | 4 | 4 | 0 | 0 | 100% |
| §2 动画语言 | 11 | 9 | 2 | 0 | 82% |
| §3 反馈语言 | 7 | 7 | 0 | 0 | 100% |
| §4 状态语言 | 9 | 5 | 3 | 1 | 56% |
| §5 层级语言 | 5 | 3 | 2 | 0 | 60% |
| §6 布局语言 | 4 | 3 | 1 | 0 | 75% |
| §7 组件交互 | 9 | 9 | 0 | 0 | 100% |
| **合计** | **49** | **40** | **8** | **1** | **82%** |

> **v1.1 更新**：基于 commit `3515e5a` 重新审计 —— BUG-005（翻页按钮 bottom 140→220rpx）**已修复** ✅，§5 通过率从 40%→60%，总通过率 80%→82%。

---

## 二、🔴 违规项（必须修复 · 1 项）

### BUG-STD-002：详情页标题缺失无兜底文案

- **文件**：`pages/detail/detail.wxml` L36
- **当前**：`<view class="detail-title">{{news.title}}</view>` — 无 `wx:if`/`wx:else`
- **标准**：§4.3 要求 "标题缺失时显示 '无标题'（灰色兜底）"
- **修复**：`<view class="detail-title">{{news.title || '无标题'}}</view>`
- **改动量**：1 行
- **关联**：可合并到前端已有的 UX-BUG09~14 批次修复

---

## 三、⚠️ 偏差项（建议修复 · 8 项）

### DEV-STD-01：跨分类闪烁条代码仍存在 → 已有任务 UX-SIMPLIFY05 📋

- **文件**：`pages/detail/detail.wxml` L23 + `detail.wxss` L52-65 + `detail.js` `_showFlash`
- **状态**：UX-SIMPLIFY05 已建任务 📋 交前端，代码尚未执行
- **说明**：与广播区"前端待办"中的 UX-BUG09~14 批次无关，是独立的简化优化任务
- **影响**：跨分类时双重视觉信号（进度指示分类名 + 闪烁条）

### DEV-STD-02：引导遮罩 CSS 残留 → 与广播区"追加清理"部分重叠

- **文件**：`pages/home/home.wxss` L396-419（`.guide-overlay` + `@keyframes fadeInOut`）
- **状态**：TASK_BOARD 广播（2026-08-01）提到"追加清理已合入 main：去掉首页引导遮罩（guide-overlay）"——**wxml 已移除 ✅，wxss 残留**
- **说明**：前端清理了 wxml 但忘了清理 wxss，约 24 行死代码
- **建议**：补充到前端下一批清理任务

### DEV-STD-03：详情页底部"← 返回"按钮仍存在 → 已有任务 UX-SIMPLIFY01 📋

- **文件**：`pages/detail/detail.wxml` L62 + `detail.wxss` L221-227（`.back-bottom`）
- **状态**：UX-SIMPLIFY01 已建任务 📋 交前端
- **说明**：广播区"追加清理"中提到"去掉详情页翻页按钮（pager-hint）"——但那是翻页按钮，不是 back-bottom。两者不同，S1 未被那批清理覆盖

### DEV-STD-04：元信息 source 缺失时仍渲染空 `<text>`

- **文件**：`pages/detail/detail.wxml` L41：`<text>{{news.source}}</text>` — 无 `wx:if`
- **标准**：§4.3 "来源缺失时隐藏"
- **修复**：`<text wx:if="{{news.source}}">{{news.source}}</text>`
- **改动量**：1 行

### DEV-STD-05：首页卡片元信息 time 缺失时仍渲染空 `<text>`

- **文件**：`pages/home/home.wxml` L74：`<text>{{item.time}}</text>` — 无 `wx:if`
- **标准**：§4.3 "时间缺失时隐藏整个 time 节点"
- **修复**：`<text wx:if="{{item.time}}">{{item.time}}</text>`
- **改动量**：1 行

### DEV-STD-06：content padding-bottom 偏小

- **文件**：`pages/detail/detail.wxss` L72：`calc(100rpx + env(safe-area-inset-bottom) + 32rpx)`
- **标准**：§5.4 要求含翻页按钮区完整高度
- **当前**：翻页按钮 bottom 已修至 220rpx ✅，但 padding-bottom 未同步调整
- **建议**：调整为 `calc(100rpx + env(safe-area-inset-bottom) + 220rpx + 40rpx)`

### DEV-STD-07：间距 28rpx 非 8 的倍数

- **文件**：多处（`home.wxss` L22 `padding: 28rpx`、L180 `right: 28rpx`、L302 `padding: 28rpx` 等）
- **标准**：§6.2 要求 "间距使用 8rpx 倍数"
- **影响**：微小视觉不统一
- **建议**：改为 24rpx 或 32rpx，优先级低

### DEV-STD-08：首页卡片 time 已修复分隔符但 time 节点缺 wx:if

- **文件**：`pages/home/home.wxml` L73-74
- **当前**：`<text wx:if="{{item.source && item.time}}"> - </text>` + `<text>{{item.time}}</text>`
- **说明**：分隔符条件已包含双重检查 ✅，time 本身无 wx:if
- **修复**：`<text wx:if="{{item.time}}">{{item.time}}</text>`

---

## 四、✅ 已由前端修复项（v1.0→v1.1 更新）

| ID | 问题 | 修复状态 |
|----|------|:---:|
| ~~BUG-STD-001~~ | 翻页按钮 bottom 140→220rpx | ✅ 已修复（detail.wxss L90: `bottom: 220rpx`） |
| ~~BUG-005~~ | 翻页按钮与操作栏重叠 | ✅ `fix/bugs-005-006` 分支已删、main 已合 |

---

## 五、✅ 完全合规项（40 项）

- 手势语言 4/4：仅用 4 种手势、WXS 触摸处理、手势冲突优先级
- 反馈语言 7/7：Toast 时长、Chip 防抖、按钮 :active、无静默操作
- 组件交互 9/9：字体面板 3s 关闭、即时生效、侧边栏宽度/遮罩关闭、收藏持久化/上限、分享预生成/截断
- 动画语言 9/11：翻页/回弹/面板/遮罩/心形/标签/Chip 动画全部合规（2 项偏差为 S5+CSS残留）
- 布局语言 3/4：触控热区、字体缩放、tier 3 截断
- 状态语言 5/9：首页四态完整、详情 error 兜底、收藏/侧边栏 empty 态
- 层级语言 3/5：z-index 范围合规、字体面板 z-index 正确、翻页按钮 bottom 已修

---

## 六、修复优先级与任务合并建议

> **原则**：前端已有 13 项积压任务（UX-BUG09~14 ×6 + UX-IMPROVE04~07 ×4 + BUG-007~009 ×3）。本走查发现的问题应尽可能合并到已有批次，避免新增任务碎片。

| 优先级 | ID | 合并建议 | 改动量 |
|:---:|------|----------|:---:|
| 🔴 P0 | BUG-STD-002 | 合并到 **UX-BUG09~14 批次**（"详情页加载"类） | 1 行 |
| 🟡 P1 | DEV-STD-04 | 合并到 **UX-BUG09~14 批次**（"高亮样式"类） | 1 行 |
| 🟡 P1 | DEV-STD-05 | 合并到 **UX-BUG09~14 批次** | 1 行 |
| 🟡 P1 | DEV-STD-08 | 合并到 **DEV-STD-05 同修** | 1 行 |
| 🟢 P2 | DEV-STD-01 | 独立任务 **UX-SIMPLIFY05**（已有 📋） | ~20 行 |
| 🟢 P2 | DEV-STD-03 | 独立任务 **UX-SIMPLIFY01**（已有 📋） | ~10 行 |
| 🟢 P2 | DEV-STD-02 | 合并到前端**下一批清理** | 24 行（删） |
| 🟢 P2 | DEV-STD-06 | 合并到 **UX-IMPROVE04~07 批次**（"正文间距"类） | 1 行 |
| 🟢 P3 | DEV-STD-07 | 独立优化，下版本 | ~8 处 |

---

## 七、总结

- **通过率 82%（40/49）**，较 v1.0 走查（80%）提升 2pp
- **BUG-005 已修复** ✅（翻页按钮 bottom 220rpx），此前 🔴 违规降级消除
- **仅剩 1 项 🔴**：标题缺失无兜底（1 行修复）
- **8 项 ⚠️**：其中 2 项已有独立任务（S1/S5），6 项可合并到前端已有批次
- **建议前端开发认领 UX-BUG09~14 批次时顺手带上 BUG-STD-002 + DEV-STD-04/05/08**（共 +4 行改动）

> **走查结论**：代码与交互语言标准 v1.0 一致性持续改善（82%），核心交互模式稳固。主要遗留问题集中在空数据降级（wx:if 遗漏）和 CSS 残留清理，改动量极小，建议与前端已有批次合并修复。
