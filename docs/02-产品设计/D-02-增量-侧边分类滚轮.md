# D-02 增量 · 侧边分类选择器交互规范（RQ-15-D v2.2）

> **编号**：RQ-15-D | **角色**：产品设计师 | **日期**：2026-08-02（v2：2026-08-04 顶部锚定范式；v2.1：2026-08-04 去线框 + 锚点第二行；v2.2：2026-08-04 温和放大 + 颜色不加深 + 字号不变；v2.3：2026-08-04 淡蓝底色保证暗色可见；**v2.4：2026-08-06 选中项字体改淡蓝 `--primary`**；v2.5：2026-08-06 选中项去背景高亮（仅淡蓝字体）；**v2.6：2026-08-06 锚点修正+全条带触摸+连续跟手**；**v2.7：2026-08-06 wheel 面板内顶部对齐，选中固定位上方仅留 1 个分类名空间**）
> **上游**：`PRD-RQ15-侧边分类滚轮选择器.md`（10 条验收 AC）| **用户决策**：2026-07-31（始终可见 / 顶部显示分类名 / 每 snap 震动）；2026-08-04 v5（顶部锚定 + 超界隐藏 + 滑动放大选择位 + 去分割线）；2026-08-04 v5.1（idle 去蓝色线框 + 锚点第二行上方保留前一个分类名）；2026-08-04 v5.2（选中位固定、其他分类滚动；滚动到固定位温和放大 scale(1.08)；颜色不加深保持选中态 .5；字号不增大）
> **⚠️ v2.7 变更**（布局）：owner 2026-08-06 16:09（真机体验 008 后反馈「侧边分类的位置要稍微再高一点，固定的分类名称展示区域上方只保留一个分类名称展示区域就可以了，不要留太多的空间」）——wheel 在侧边栏面板内由**垂直居中**改为**顶部对齐**（`.panel-wheel` `align-items: center → flex-start`，`padding: 20rpx 0 → 8rpx 0`）：选中固定位（第 2 行）上方仅保留第 1 行一个分类名空间，移除居中带来的大段空白。选中锚点（第 2 行）/项高 72rpx/可视 6 行/触摸热区/连续跟手均不变。
> **⚠️ v2.6 变更**（交互提升）：owner 2026-08-06 15:12——① **选中锚点修正**：选中分类恒固定在容器**第 2 行（顶部恒留 1 个分类名空间，首类时留空行）**——当前实现 `Math.max(0, idx-anchorIndex)` 导致 idx≤1 时选中项贴顶，需改为 `ty = (anchorIndex - idx) × itemHeight`（不 clamp）；② **触摸热区**：整个滚轮条带（宽 128rpx × 可视 6 行高）+ 合理外扩区域均可触摸滚动，**不限于分类文字命中**；③ **滚动连续性**：拖动时列表位移**连续跟随手指**（非按格跳变），松手 300ms 缓动 snap 到最近分类（越界 bounce 保留）。
> **⚠️ v2.5 变更**（追修）：owner 2026-08-06 11:52——选中项**去背景高亮**（`background-color`/`border-radius` 移除），**仅保留淡蓝字体**（`--wheel-text-selected-color` 浅 `#007AFF` / 暗 `#0A84FF`）；`--wheel-bg-selected` token 定义保留但不再引用（避免破坏 v13 契约）；触摸放大 scale(1.08)/锚点/项数/手势不变。
> **⚠️ v2.4 变更**：owner 2026-08-06（真机走查）① idle 选中项**字体改淡蓝 `--primary`**（浅色 `#007AFF` / 暗色 `#0A84FF`），淡蓝底色 `--wheel-bg-selected` 当时保留；② 其余全部以当前实现为准——**panel-wheel 保留在侧边栏内、超界隐藏、项数以当前 5 项为准、触摸手势/active 触摸态/选中锚点均保留**（AC-RQ15-10「侧边栏不再显示分类 Tab」本版本**不执行**，经 owner 确认侧边栏保留分类滚轮）。
> **下游**：视觉设计师（RQ-15-UI 暗色适配）→ 技术负责人（RQ-15-T 可行性）→ 前端开发（RQ-15-FE 组件）
> **对齐**：[交互语言标准 v1.0](./D-02-交互语言标准-v1.0.md)（§1 手势 / §2 动画 / §3 反馈 / §4 状态 / §5 层级 / §6 布局）
> **红线**：SOP v1.6 §15.4 — WXS 纯 ES5、touch 处理禁 `return false`；手势隔离不得阻断卡片流 WXS 手势。
> **⚠️ v2 变更**：owner 2026-08-04（UI-B7 v5）将选中锚点从「垂直居中」改为「**顶部锚定**」、删除分割线、激活态由「主题色高亮」改为「scale(1.15) 放大选择位」。本规范 v2 即按新范式修订。
> **⚠️ v2.1 变更**：owner 2026-08-04（UI-B7 v5.1）① idle 选中项**去掉蓝色圆角线框**，仅字体颜色加重；② 选中项固定**第二行**（上方保留前一个分类名，非贴顶）。
> **⚠️ v2.2 变更**：owner 2026-08-04（UI-B7 v5.2）① 选中分类位置**固定**，其他分类围绕其上下滚动；② 滚动到固定位时**温和放大 scale(1.08)**（原 1.15 回调）；③ 激活态字体颜色**保持选中态 `.5` 不加深**（原 `.6` 回调）；④ 字号**保持选中态 24rpx 不增大**。
> **⚠️ v2.3 变更**：owner 2026-08-04（UI-B7 v5.3-final）① idle 选中项加**淡蓝底色 `var(--highlight-bg)`**（浅色 `rgba(0,122,255,.06)` / 暗色 `rgba(10,132,255,.10)`）保证暗色模式可见；② 确认固定位第二行不可移动。

---

## 1. 组件定位与结构

- **形态**：新建 `components/category-wheel/` 独立组件，`home.wxml` 右侧引用，不侵入现有 `home.js` 的 `loadNews` / `onCategoryChange` 核心逻辑（仅新增触发入口）。
- **尺寸**（符合标准 §6 触控热区下限 88rpx）：宽 **88rpx**，距卡片流右缘 **8rpx**，与卡片流等高。
- **内容**：9 项 = 8 分类（推荐/科技/国际/体育/生活/三农/科学）+ ❤️收藏；「❤️收藏」上方 **无分割线（v2 删除，owner：选中项已有高亮）**。
- **选中锚点（v2.2 · v2.6 修正）**：**选中分类位置固定**（顶部锚定列表范式，替代原垂直居中滚轮）：选中项固定容器**第二行**，**上方恒保留一个分类名空间**（`translateY = (anchorIndex - curCat) × 项高`，anchorIndex=1；**v2.6 去掉 `Math.max(0,·)` clamp**——首类 curCat=0 时列表下移 1 行、顶部留空行，保证选中项恒在第 2 行不贴顶）。**其他分类围绕固定位上下滚动**。可视区默认 6 项，超出隐藏，滑动滑出。
- **层级**（标准 §5）：滚轮 `z-index` 高于卡片流、低于顶部栏与字体面板；具体数值沿用标准 §5 的 14 层序列表。

---

## 2. 视觉状态机（三态 · v2.2 修订）

### 2.1 默认态（idle · 半透明驻留 · v2.3 修订）
`opacity 0.35`，透明背景，无指示线。

| 元素 | 浅色模式 | 暗色模式 |
|------|---------|---------|
| 非选中文字 | `rgba(0,0,0,0.12)`，20rpx | `rgba(255,255,255,0.12)`，20rpx |
| 选中文字（v2.5） | **`var(--primary)`（`#007AFF`）淡蓝**，24rpx，weight 500，**无底色**（v2.5 去背景高亮），无线框 | **`var(--primary)`（`#0A84FF`）淡蓝**，24rpx，weight 500，**无底色**（v2.5 去背景高亮），无线框 |

> v2.1 删除原 idle 选中项蓝色圆角线框（`border: 1px solid var(--primary)` + radius + 淡色底），仅保留字体颜色（owner：不需要蓝色线框）。
> v2.2 将 idle 选中项颜色从 `.6/weight 600` 回退为 `.5/weight 500`（owner：字体颜色不需要加深，就还是当前的选中态字体颜色）。
> **v2.3 增加淡蓝底色**（owner：淡淡蓝色高亮一下，不让在夜晚模式下看不清）。
> **v2.4 选中项字体改淡蓝 `--primary`**（owner 2026-08-06：选中态期望是字体淡蓝色）；淡蓝底色保留；其余（锚点/手势/项数/超界隐藏）均以当前实现为准。
> **v2.5 去背景高亮**（owner 2026-08-06 11:52：字体淡蓝的同时，把背景高亮去掉）——选中项无底色，仅淡蓝字体；触摸放大/锚点/项数/手势不变。

### 2.2 激活态（active · 触摸 · v2.2 修订）
`opacity 0.92`。**v2.2：不做高亮（无背景/边框/主题色/指示线），仅温和放大表示选择位；颜色不加深、字号不增大**。

| 元素 | 浅色模式 | 暗色模式 |
|------|---------|---------|
| 非选中文字 | `rgba(0,0,0,0.45)`，22rpx，`opacity .35` | `rgba(255,255,255,0.45)`，22rpx，`opacity .35` |
| 选中文字（选择位） | **`transform: scale(1.08)` 温和放大**（v2.2 回调，原 1.15）；**颜色保持选中态 `rgba(0,0,0,.5)` 不加深、字号保持 24rpx 不增大**；无背景无边框无主题色 | 同左（暗色 `.5`） |
| 过渡 | 激活态 150ms ease-out（快速跟手） | — |

### 2.3 过渡（标准 §2 动画 ≤500ms）
`idle ↔ active`：进入激活 150ms ease-out；**手指离开后 300ms 恢复默认态**（持续停留不滑动则保持激活态）。

---

## 3. 手势交互（对齐标准 §1 手势词汇 · v2.2 顶部锚定第二行）

- **词汇约束**：仅用「垂直拖拽」单手势 + 松手 `snap`，**不引入新手势**（保持标准 §1 的 4 手势体系）。
- **顶部锚定（v2.2）**：**选中分类位置固定**在容器**第二行**（上方保留前一个分类名）；**其他分类围绕固定位上下滚动**；可视区 6 项，超出隐藏；滑动时列表整体位移，超界项滑出/滑入。
- **touchstart**：激活态过渡启动；记录起始 Y。
- **touchmove**：实时映射 Y 偏移 → 分类索引（`当前偏移 / 每占位高` 四舍五入取整），**与上次不同才 `setData`（防抖去重）**；实时切换主卡片流（不等松手）。
- **touchend**：`snap` 吸附到**容器第二行**（选中项对齐第二行，`translateY = max(0, -(curCat-1) × 项高)`）+ `wx.vibrateShort({ type: 'light' })`。
- **惯性限制**：单次惯性最多跨 **3 个分类**后减速停止（防一滑到底）。
- **边界**：首/末项继续滑 → 轻微 `bounce` 弹跳，不切换。
- **🚫 红线**：touch 处理禁 `return false`；滚轮区域 `catchtouchmove` 消费事件，不冒泡到卡片流 WXS。

---

## 4. 反馈语言（对齐标准 §3）

- **切换反馈**：卡片流即时切换（乐观更新，无 loading 遮罩）。
- **震动反馈**：每次 `snap` → `wx.vibrateShort({ type: 'light' })`（轻量触感，符合标准 §3「轻量震动」）。
- **分类标题**（卡片流顶部居中）：26rpx，weight 500，主题色 `opacity 0.7`（浅）/ `0.8`（暗），与卡片流顶距 12rpx，切换 **200ms 淡入淡出**。
  - 选中「❤️收藏」→ 标题「我的收藏」+ 卡片流切收藏列表视图。

---

## 5. 暗色模式 Token 表（PRD §4.2/§4.3 rgba → 标准 token）

> 将 PRD 的具体 rgba 整理为设计 token，供视觉（RQ-15-UI）出暗色适配稿、前端（RQ-15-FE）引用。

| Token | 浅色 | 暗色 |
|-------|------|------|
| `--wheel-opacity-idle` | `0.35` | `0.35` |
| `--wheel-opacity-active` | `0.92` | `0.92` |
| `--wheel-text-idle` | `rgba(0,0,0,0.12)` | `rgba(255,255,255,0.12)` |
| `--wheel-text-selected-idle`（v2.4） | **`var(--primary)`（`#007AFF`）**，weight 500，**底色 `rgba(0,122,255,.06)` 保留** | **`var(--primary)`（`#0A84FF`）**，weight 500，**底色 `rgba(10,132,255,.10)` 保留** |
| `--wheel-text-active` | `rgba(0,0,0,0.45)` | `rgba(255,255,255,0.45)` |
| `--wheel-scale-active`（v2.2） | `scale(1.08)` | `scale(1.08)` |
| `--wheel-anchor` | 第二行（`max(0, -(curCat-1) × 36px)`） | 同左 |
| ~~`--wheel-chip-border`~~ | ~~`1px solid var(--primary)`~~（v2.1 删除：idle 去线框） | ~~同左~~（v2.1 删除） |
| ~~`--wheel-divider`~~ | ~~`rgba(0,0,0,0.2)`~~（v2 删除） | ~~`rgba(255,255,255,0.2)`~~（v2 删除） |

> v2 变更：`--wheel-text-selected-active` / `--wheel-bg-active` / `--wheel-indicator` 已废弃（激活态不再高亮，仅放大选择位）；`--wheel-divider` 已废弃（去分割线）。
> v2.1 变更：`--wheel-chip-border` 已废弃（idle 去蓝色线框）；新增 `--wheel-anchor`（选中项固定第二行）。
> v2.2 变更：`--wheel-text-selected-idle` 回退至 `.5/500`（颜色不加深）；`--wheel-scale-active` 回调至 `scale(1.08)`（温和放大）。
> **v2.3 变更**：`--wheel-text-selected-idle` 增加淡蓝底色（`rgba(0,122,255,.06)` 浅 / `rgba(10,132,255,.10)` 暗）保证暗色可见。
> **v2.5 变更**：选中项去背景高亮，`--wheel-bg-selected` 定义保留（v13 契约）但组件不再引用。
> **v2.4 变更**：`--wheel-text-selected-idle` **文字色改 `var(--primary)` 淡蓝**（浅 `#007AFF` / 暗 `#0A84FF`），底色保留；其余（panel-wheel 保留侧边栏内 / 超界隐藏 / 项数 5 项 / 触摸手势 / active 触摸态 / 选中锚点）以当前实现为准。

| 字号 | 20 / 22 / 24 rpx（非选中 20→22、选中 24；**激活态字号不变仅 scale 放大**） |
|------|------|
| 字重 | 非选中 400 / 选中 500（idle）→ 500（active，不加重） |
| 过渡 | 激活 150ms ease-out / 恢复 300ms ease-out（状态切换）/ 200ms ease（标题淡入） |

---

## 6. 与卡片流 WXS 手势隔离（关键）

- 滚轮 `touchstart/move/end` 在组件内 **`catch` 消费**，不向上冒泡。
- 卡片流左右滑动 WXS 不受影响（手势冲突优先级：滚轮区域优先于卡片流，标准 §1）。
- **真机校验项**：滚轮拖动不触发卡片翻动；卡片拖动不触发滚轮切换。

---

## 7. 验收映射（AC-RQ15-01~10 → 本规范条款）

| AC | 验收项 | 对应条款 |
|----|--------|----------|
| AC-RQ15-01 | 首页加载右侧出现半透明分类栏，选中项固定第二行（上方保留前一个分类名） | §1 结构 / §2.1 默认态 |
| AC-RQ15-02 | 触碰 150ms 内激活（选中项 scale(1.15) 放大 + 字重，无高亮背景） | §2.2 / §2.3 |
| AC-RQ15-03 | 离开 300ms 后恢复半透明 | §2.3 |
| AC-RQ15-04 | 滑动即时切换 + 松手 snap 到第二行 + 震动 | §3 / §4 |
| AC-RQ15-05 | 惯性最多跨 3 类后停止 | §3 惯性限制 |
| AC-RQ15-06 | 首/末类继续滑 bounce 不切换 | §3 边界 |
| AC-RQ15-07 | 选中「❤️收藏」展示收藏列表 + 标题「我的收藏」 | §4 分类标题 |
| AC-RQ15-08 | 分类栏运作不冲突卡片手势 | §6 手势隔离 |
| AC-RQ15-09 | 暗色模式分类栏适配 | §5 Token 表 |
| AC-RQ15-10 | 分类栏上线后侧边栏不再显示分类 Tab | ~~下游 F-RQ15-06（home.wxml 移除 `panel-tabs`）~~ **v2.4 不执行**（owner 2026-08-06：侧边栏保留 panel-wheel，分类滚轮就在侧边栏内） |
| AC-RQ15-11（v5 新增） | 可视区 6 项，超界分类隐藏，滑动滑出 | §1 选中锚点 |
| AC-RQ15-12（v5 新增） | 分类间无分割线 | §1 内容 |
| AC-RQ15-13（v2.1 新增） | idle 选中项无线框无底色，颜色不加深（.5/500） | §2.1 |
| AC-RQ15-14（v2.1 新增） | 选中项固定第二行，上方保留前一个分类名 | §1 选中锚点 / §3 |
| AC-RQ15-15（v2.2 新增） | 选中分类位置固定，其他分类围绕固定位上下滚动 | §1 选中锚点 / §3 |
| AC-RQ15-16（v2.2 新增） | 滚动到固定位时仅温和放大 scale(1.08)，颜色不加深、字号不增大 | §2.2 / §5 |
| AC-RQ15-17（v2.3 新增 · **v2.5 修订**） | idle 选中项**无背景高亮**（v2.5 去底色），仅淡蓝字体 `var(--primary)`；暗色下字体可见 | §2.1 / §5 |
| AC-RQ15-18（v2.6 新增） | 选中分类**恒在第 2 行**：首类（全部）时顶部留空行不贴顶；次类（科技）时顶部显示「全部」 | §1 选中锚点 |
| AC-RQ15-19（v2.6 新增） | 触摸热区 = 整个滚轮条带（128rpx × 可视 6 行）+ 合理外扩；触摸条带空白/文字/行间隙均响应滚动，**不限于分类文字命中** | §3 手势 |
| AC-RQ15-20（v2.6 新增） | 拖动时列表位移**连续跟随手指**（按格跳变为禁止项）；松手 300ms 缓动 snap 最近分类；越界 bounce 保留 | §3 手势 |

---

## 8. 交接说明

- **视觉设计师（RQ-15-UI）**：基于 §5 Token 表产出暗色适配视觉稿；确认毛玻璃 `blur(8px)` 在目标基础库的渲染表现。
- **技术负责人（RQ-15-T）**：评估 `scroll-snap-type: y mandatory`（基础库 3.0.0+）可行性；惯性 / `bounce` 实现方式；WXS 手势隔离的 ES5 合规写法。
- **前端开发（RQ-15-FE）**：实现 `components/category-wheel/`，不改 `home.js` `loadNews`/`onCategoryChange` 核心逻辑；实时切换用 `bindtouchmove` + 去重 `setData`。
- **关注人**：产品经理（PRD 作者）+ 前端开发 + 视觉设计师 + 技术负责人（建单时添加，便于及时收通知）。

---

## 9. 首页交互增量（用户直报 BUG-20260802-005 / 006）

> 以下两项为 2026-08-02 用户直报缺陷对应的交互规范，补充于本交互稿便于首页交互规范统一（广播指派交互设计师文档化）。

### 9.1 BUG-20260802-005 · 设置按钮 3D 浮动（卡片右下角）
- **位置**：从顶部栏右侧（`top-bar-settings`）移至卡片右下角浮动层，距右缘/下缘各 **24rpx**。
- **形态**：圆形浮动按钮（88rpx 触控热区），带 3D 投影 `box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.18)`（暗色 `rgba(0,0,0,0.4)`）。
- **动效**（标准 §2 ≤500ms）：入场 `fade + scale 0.9→1` 300ms ease-out；点击 `:active` `scale 0.94` 150ms；常驻不随卡片滚走（与滚动解耦）。
- **层级**：高于卡片流、低于字体面板/分享卡片（沿用标准 §5 序列表）。

### 9.2 BUG-20260802-006 · 0.5s 分类切换提示
- **触发**：首页 `onCategoryChange` / 详情跨分类 `showCrossingCategory`。
- **样式**：分类名以当前主题色高亮，**120ms 淡入 → 维持 → 600ms 后淡出**（v6.2 修正，PD 2026-08-05 批准）。
  > **⚠️ v6.2 变更依据**：原「500ms 展示 + 200ms 淡入淡出」有效可见窗口仅 `500 − 2×200 = 100ms`，实际不可读——这是 BUG-20260802-006 反复重开的根因。改为「600ms 展示 + 120ms 淡入淡出」后，有效窗口 `600 − 2×120 = 360ms`，提升 3.6×。详情端 `detail.js` 同步。
- **与 §4 关系**：滚轮选中分类的卡片流顶部标题（200ms 淡入）为本项在 RQ-15 场景的具体落地；二者共用「主题色 + 淡入淡出 + ≤500ms」反馈语言（标准 §3）。

---

> **任务归属**：本报告为 **RQ-15-D（侧边分类滚轮交互增量）** 交付物，依赖「无（PRD 就绪）」已满足。下一步激活 RQ-15-UI → RQ-15-T → RQ-15-FE 依赖链。另含 BUG-20260802-005/006 交互规范（用户直报，广播指派交互设计师文档化）。
