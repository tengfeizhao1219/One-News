# 接力棒

> **这是多会话自动推进的核心机制。**
> 每个会话完成工作后更新本文件。下一个会话读取本文件就知道该做什么。
> 你只需要对新会话说一句「接棒」，它就会自动读取本文件并继续推进。

---

## 🔴 当前接力棒

### 角色激活卡
- **身份**：产品经理
- **可以**：读写 `docs/` 目录、创建需求文档、更新 `TASK_BOARD.md`
- **不可以**：修改代码、合并分支、推送 main
- **上级**：项目经理（超出权限的决策 → 标记阻塞，不要自行决定）

### 任务
- 任务 ID：R-01 ~ R-06
- 要做的事：完成阶段一需求规划全流程
- 上一棒产出：无（项目初始）

### 完成标准（Checklist）
- [ ] `docs/iteration-0/用户画像.md` 已创建
- [ ] `docs/iteration-0/竞品分析报告.md` 已创建
- [ ] `docs/iteration-0/需求池.md` 已创建
- [ ] `docs/iteration-0/需求优先级矩阵.md` 已创建
- [ ] `docs/iteration-0/需求评审纪要.md` 已创建
- [ ] `TASK_BOARD.md` 中 R-01~R-06 标记为 ✅

### 启动指令
```
git pull origin main，读 RELAY.md，以产品经理身份接棒，执行阶段一需求规划
```

---

## 📦 交接包（上一棒完成后填写）

```markdown
## 阶段X完成 | 角色：XXX → 下一棒：YYY

### 我做了什么
- （要点列表）

### 产出物清单
| 文件 | 路径 |
|------|------|
| XXX | docs/... |
| YYY | docs/... |

### 给下一棒的特别提醒
- （关键背景、注意点、关联约束）
```

---

## 🚪 关口检查（每棒完成后必须经过）

```
[执行角色] 完成 → [项目经理] 关口检查 → [下一棒] 接棒
```

项目经理关口检查指令：
```
接棒，审查[阶段X]产出，检查完成标准是否满足，满足则推进下一棒，不满足则回退
```

---

## 🔙 回退棒（异常处理）

如果发现上一棒产出有问题：
1. **不要覆盖**上一棒的工作
2. 在 `TASK_BOARD.md` 标记对应任务为 🚫
3. 在阻塞项写明问题
4. 更新 RELAY.md，创建「修复棒」指回上一棒角色
5. 告诉用户："阶段X产出有问题，请接修复棒"

---

## 🔀 并行棒（多角色同时推进）

```markdown
## 🔴 当前接力棒（并行模式）

┌─────────────────────────────────────────┐
│ 棒 A：前端开发                            │
│   任务：B-02 首页开发                     │
│   分支：feature/home-page                │
│   启动指令：接棒，前端开发，开发首页       │
├─────────────────────────────────────────┤
│ 棒 B：后端开发                            │
│   任务：B-03 云函数开发                   │
│   分支：feature/cloud-functions          │
│   启动指令：接棒，后端开发，开发云函数     │
├─────────────────────────────────────────┤
│ 两根棒都完成后 → 项目经理接合并棒          │
└─────────────────────────────────────────┘
```

---

## 📜 接力历史

| # | 时间 | 角色 | 做了什么 | 下一棒 |
|---|------|------|----------|--------|
| 0 | 07-30 | 项目经理 | 搭建协作框架 + 接力棒机制 | → 产品经理 |

---

## 🎯 完整流水线

```
阶段一          阶段二         阶段三         阶段四         阶段五         阶段六
需求规划  ──→  产品设计  ──→  技术方案  ──→  开发实现  ──→  测试验收  ──→  上线复盘
   │               │              │              │              │              │
产品经理        产品经理       技术负责人     项目经理       项目经理       项目经理
   │           设计师         前端/后端      前端/后端      测试工程师       全员
   │                          项目经理      技术负责人     产品经理
```

---

## ⚙️ 使用规则

### 接棒（新会话启动）：
1. `git pull origin main`
2. 读 RELAY.md → 看「角色激活卡」+「任务」+「完成标准」
3. 读 CONTEXT.md → 了解项目
4. 读 TASK_BOARD.md → 认领任务
5. 按启动指令开始工作

### 交棒（会话完成）：
1. 对照「完成标准」逐条自查
2. 在 COMMLOG.md 写「交接包」
3. 更新 RELAY.md → 角色激活卡 + 任务 + 完成标准 + 启动指令
4. 更新 TASK_BOARD.md → 标记完成
5. `git commit + push`
6. 告诉用户："请接棒，下一棒是 [角色]"

---

> **最后更新**：2026-07-30 | **维护者**：项目经理 | **版本**：v1.1
