# 接力棒

> **这是多会话自动推进的核心机制。**
> 每个会话完成工作后更新本文件。下一个会话读取本文件就知道该做什么。
> 主线用「接力棒」推进；**非棒状态你也能随时对任意角色直接提诉求（即席/直连模式）**，角色按权限处理并轻量同步，不碰棒。
> 触发不限死板关键词——「接棒 / 继续 / 该你 / 开始阶段X / 你来做 …」都算，角色自动判别主线还是支线。

---

## 🔴 当前接力棒

### 角色激活卡
- **身份**：产品经理
- **可以**：读写 `docs/` 目录、创建需求文档、更新 `TASK_BOARD.md`
- **不可以**：修改代码、合并分支、推送 main
- **上级**：项目经理（超出权限的决策 → 标记阻塞，不要自行决定）

### 任务
- 任务 ID：R-01 ~ R-06
- 要做的事：完成阶段一需求规划全流程
- 上一棒产出：无（项目初始）

### 完成标准（Checklist）
- [ ] `docs/01-需求规划/用户画像.md` 已创建
- [ ] `docs/01-需求规划/竞品分析报告.md` 已创建
- [ ] `docs/01-需求规划/需求池.md` 已创建
- [ ] `docs/01-需求规划/需求优先级矩阵.md` 已创建
- [ ] `docs/01-需求规划/需求评审纪要.md` 已创建
- [ ] `TASK_BOARD.md` 中 R-01~R-06 标记为 ✅
- [ ] 🔒 `CONTEXT.md`「最新提交」已更新为本次 commit hash
- [ ] 🔒 `COMMLOG.md` 已追加交接包（做了什么 + 产出物 + 提醒）
- [ ] 🔒 `RELAY.md`「当前接力棒」已更新指向下一棒角色

### 启动指令

**执行阶段：**
git pull origin main，读 RELAY.md，以产品经理身份接棒，执行阶段一需求规划。

**交付阶段（任务完成后必须执行，缺一不可）：**
1. 对照 RELAY.md「完成标准」逐条自查（包括 🔒 强制项）
2. 在 COMMLOG.md 追加交接包（做了什么 + 产出物清单 + 给下一棒的提醒）
3. 更新 CONTEXT.md「最新提交」为本次 commit hash
4. 更新 RELAY.md「当前接力棒」指向下一棒角色（含角色激活卡 + 任务 + 完成标准 + 启动指令）
5. 更新 TASK_BOARD.md 标记对应任务为 ✅
6. git add + commit + push
7. 全部做完后告诉用户："请接棒，下一棒是 [角色]"

> ⚠️ 以上 7 步全部完成才算本棒结束。不可跳过任何一步。

---

## 📦 交接包（上一棒完成后填写）

```markdown
## 阶段X完成 | 角色：XXX → 下一棒：YYY

### 我做了什么
- （要点列表）

### 产出物清单
| 文件 | 路径 |
|------|------|
| XXX | docs/... |
| YYY | docs/... |

### 给下一棒的特别提醒
- （关键背景、注意点、关联约束）
```

---

## 🚪 关口检查（每棒完成后必须经过）

```
[执行角色] 完成 → [项目经理] 关口检查 → [下一棒] 接棒
```

项目经理关口检查指令：
```
接棒，审查[阶段X]产出，检查完成标准是否满足，满足则推进下一棒，不满足则回退
```

---

## 🔙 回退棒（异常处理）

如果发现上一棒产出有问题：
1. **不要覆盖**上一棒的工作
2. 在 `TASK_BOARD.md` 标记对应任务为 🚫
3. 在阻塞项写明问题
4. 更新 RELAY.md，创建「修复棒」指回上一棒角色
5. 告诉用户："阶段X产出有问题，请接修复棒"

---

## 🔀 并行棒（多角色同时推进）

```markdown
## 🔴 当前接力棒（并行模式）

┌─────────────────────────────────────────┐
│ 棒 A：前端开发                            │
│   任务：B-02 首页开发                     │
│   分支：feature/home-page                │
│   启动指令：接棒，前端开发，开发首页       │
├─────────────────────────────────────────┤
│ 棒 B：后端开发                            │
│   任务：B-03 云函数开发                   │
│   分支：feature/cloud-functions          │
│   启动指令：接棒，后端开发，开发云函数     │
├─────────────────────────────────────────┤
│ 两根棒都完成后 → 项目经理接合并棒          │
└─────────────────────────────────────────┘
```

---

## 📜 接力历史

| # | 时间 | 角色 | 做了什么 | 下一棒 |
|---|------|------|----------|--------|
| 0 | 07-30 | 项目经理 | 搭建协作框架 + 接力棒机制 | → 产品经理 |

---

## 🎯 完整流水线

```
阶段一          阶段二         阶段三         阶段四         阶段五         阶段六
需求规划  ──→  产品设计  ──→  技术方案  ──→  开发实现  ──→  测试验收  ──→  上线复盘
   │               │              │              │              │              │
产品经理        产品经理       技术负责人     项目经理       项目经理       项目经理
   │           设计师         前端/后端      前端/后端      测试工程师       全员
   │                          项目经理      技术负责人     产品经理
```

---

## ⚙️ 使用规则

### 角色如何响应你的消息（意图识别）

角色会话收到你的任何消息，先判意图再行动：

| 你说了什么 | 角色判定 | 走哪条线 |
|------------|----------|----------|
| 「接棒 / 继续 / 下一步 / 该你了 / 开始阶段一 / 你来做需求规划 / go / 开始吧 …」且 RELAY 当前棒=该角色 | 棒任务触发 | **主线（接力棒模式）** |
| 对该角色的具体诉求（改接口、问架构、加需求、看文件…） | 即席请求 | **支线（即席/直连模式）** |
| 「接棒」等棒触发词，但 RELAY 当前棒≠该角色 | 误用棒触发 | 提示正确会话，并转入即席处理 |
| 超出该角色权限 | 越权 | 标记阻塞/上报，不自行决定 |

> 「接棒」只是**代表性触发词**，不是命令。自然语言等价表达都算；歧义时角色会问一句确认，不死板拒绝也不误启动。

### 接棒（主线任务触发时）：

**第一步：身份核对**

每个会话在初始化时已经确定了自己的角色（见 ROLE_CARDS.md）。接棒时：

```
1. 读取 RELAY.md「当前接力棒」中的角色
2. 对比自己的角色身份
3. 如果一致 → 继续执行任务
4. 如果不一致 → 回复用户：
   "当前接力棒需要的是 [角色X]，我是 [角色Y]，请用 [角色X] 会话接棒"
   然后停止，不做任何工作
```

**示例**：
- 你是用「产品经理」会话接棒，但 RELAY.md 显示当前需要「测试工程师」
- 你应该说："当前接力棒需要的是测试工程师，我是产品经理会话，请使用测试工程师会话接棒"

**第二步：开始工作（身份核对通过后）**：
1. `git pull origin main`
2. 读 RELAY.md → 看「角色激活卡」+「任务」+「完成标准」
3. 读 CONTEXT.md → 了解项目
4. 读 TASK_BOARD.md → 认领任务
5. 按启动指令开始工作

### 支线：即席/直连模式（非棒状态也能对话）

**适用**：你随时对任意角色的直接诉求——问答、小改、临时新需求、看文件、评审代码… 不在 RELAY 主线上。
**规则**：
1. **权限不变**：即席请求仍受角色卡约束（后端不能合 main、测试不能改业务代码…）。
2. **轻量交付**（仅当动了代码/文档）：`git pull` → 完成 → 若产出物变更则 `git commit + push`（别人 `git pull` 可见）→ COMMLOG 追加一行即席记录。
3. **不更新 RELAY.md 的棒**，不打乱主线。
4. 若即席任务后来确认该进主线，由项目经理登记进 TASK_BOARD + 写新棒。
5. 纯问答直接回答，无需提交。

> 💡 即席模式让框架既保持主线严谨，又不失灵活——协调本质（文件同步）始终保留。

### 交棒（主线任务完成时）：
1. 对照「完成标准」逐条自查（包括 🔒 强制项）
2. 在 COMMLOG.md 写「交接包」
3. 更新 RELAY.md → 角色激活卡 + 任务 + 完成标准（含 🔒 强制项）+ 启动指令
4. 更新 TASK_BOARD.md → 标记完成
5. 🔒 确认 CONTEXT.md「最新提交」已更新
6. `git commit + push`
7. 告诉用户："请接棒，下一棒是 [角色]"

> ⚠️ **🔒 标记的条目是框架强制项，不可省略。** 任何一条 🔒 没完成 = 本棒未完成，不能交棒。

---

> **最后更新**：2026-07-30 | **维护者**：项目经理 | **版本**：v1.2
