# 接力棒（Relay Baton）

> **这是多会话自动推进的核心机制。**
> 每个会话完成工作后，必须更新本文件。下一个会话读取本文件就知道该做什么。
> 你只需要对下一个会话说一句「接棒」，它就会自动读取本文件并继续。

---

## 🔴 当前接力棒

```
┌─────────────────────────────────────────────────────┐
│                                                     │
│   🏃 当前阶段：阅读模式开发                          │
│   📋 任务 ID：T-01                                  │
│   👤 下一棒：[dev] 开发会话                          │
│   📄 分支：feature/reading-mode                     │
│                                                     │
│   启动指令（复制到新会话）：                          │
│   "git pull origin main，读 RELAY.md 接棒，          │
│    开发阅读模式功能"                                  │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 📜 接力历史（最近 5 棒）

| # | 时间 | 会话 | 做了什么 | 下一棒 |
|---|------|------|----------|--------|
| 0 | 07-30 | `[主]` | 搭建协作框架 + 接力棒机制 | → `[dev]` T-01 |

---

## 🔁 标准流水线

```
[dev] 开发 → [test] 测试 → [主] 审查合并 → [主] 上线
  │              │              │
  └─ 完成后 ────→┘              │
                  └─ 完成后 ────→┘
                                  └─ 完成后 → 下一个功能
```

---

## ⚙️ 接力棒使用规则

### 对于「完成工作」的会话：
1. 完成开发/测试/审查
2. 更新本文件，填写「当前接力棒」区域
3. 将下一棒的角色和任务写清楚
4. 提供可直接复制的「启动指令」
5. `git commit + push`

### 对于「接棒」的会话：
1. `git pull origin main`
2. 读取本文件（RELAY.md）
3. 读取「当前接力棒」区域
4. 读取 CONTEXT.md + TASK_BOARD.md
5. 复制「启动指令」中的描述，开始工作
6. 完成后，更新接力棒指向下一环节

### 对于你（用户）：
只需对新会话说：
> "接棒，继续推进"

或者更具体：
> "git pull，读 RELAY.md，接棒执行测试"

---

## 🎯 完整流水线示例

```
棒 #1: [dev] 开发阅读模式
  → 完成 → 更新 RELAY.md，下一棒：[test]
  → git push

你开新会话："接棒，执行测试"
  ↓
棒 #2: [test] 执行回归测试
  → 通过 → 更新 RELAY.md，下一棒：[主]
  → git push

你开新会话："接棒，审查合并"
  ↓
棒 #3: [主] 代码审查 + 合并 main
  → 完成 → 更新 RELAY.md，下一棒：[dev] T-02
  → git push

...循环...
```

---

> **设计原则**：每棒只做一件事。做完了就交棒，不贪多。
> **最后更新**：2026-07-30 | **维护者**：主会话
