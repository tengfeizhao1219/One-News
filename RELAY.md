# 既定任务跟踪表（原「接力棒」）

> **定位变更**：RELAY 从「阻塞式接力棒」转为 **既定任务跟踪表**——仅作计划内任务线性交接链的**全景视图**，不再阻塞推进。
> **实际执行**：所有任务通过 `TASK_BOARD.md` 认领与交付（拉式）。本文件用于追踪「谁该在何时接谁的交付物」。
> **即席/直连模式**保留：随时对任意角色直接提诉求，权限不变、轻量交付、不碰本表。
> **PM 关口检查（方案 A）保留**：阶段级任务全 ✅ 后，项目经理做关口检查，通过才开放下一阶段任务。

---

## 📊 既定任务跟踪表（计划内流水线）

```
阶段一 需求规划  [PM]  R-01~R-06  🔴📋 ─┐
                                            ├─→ 阶段二 产品设计
阶段二 产品设计  [UI]  D-03 🔴⏳          │   [UX] D-02 ⏳ → [PM] D-01/D-04 ⏳
                 [UX]  D-02 ⏳            │
                 [PM]  D-01 ⏳, D-04 ⏳   ├─→ 阶段三 技术方案 [TL] T-01~T-05 ⏳
                                            │
阶段三 技术方案  [TL]  T-01~T-05 ⏳  ──────┤
                                            ├─→ 阶段四 开发实现
阶段四 开发实现  [FE] B-02 🔴⏳            │   [BE] B-03 🔴⏳ → [TL] B-05 ⏳
                 [BE] B-03 🔴⏳            │
                 [TL] B-01 ⏳, B-05 ⏳     ├─→ 阶段五 测试验收 [QA] Q-01~Q-06 ⏳
                                            │
阶段五 测试验收  [QA]  Q-01~Q-03 🔴⏳      │
                 [PJM] Q-04,Q-06 ⏳        │
                 [PM]  Q-05 ⏳             ├─→ 阶段六 上线复盘 [PJM] L-01~L-05 ⏳
                                            │
阶段六 上线复盘  [PJM] L-01~L-05 ⏳  ──────┘
```

> 状态、认领人、优先级以 `TASK_BOARD.md` 为准。本表仅作阶段归属与交接顺序的同步视图。

---

## 📌 即席请求（任意角色，非主线）

> 你随时可对任意角色直接提诉求（问答、小改、临时新需求、看文件、评审…），不受本表当前阶段限制。
> 规则：权限不变；仅当动了产出物才 `git commit + push` + 记 `COMMLOG`；不更新本表；纯问答直接答。

---

## 📦 交接包（完成交接类任务时填写）

```markdown
## 阶段X完成 | 角色：XXX → 下一棒：YYY

### 我做了什么
- （要点列表）

### 产出物清单
| 文件 | 路径 |
|------|------|
| XXX | docs/... |
| YYY | docs/... |

### 给下一棒的特别提醒
- （关键背景、注意点、关联约束）
```

---

## 🚪 关口检查（PM 方案 A，保留）

```
[执行角色] 完成本阶段全部任务 → [项目经理] 关口检查（核对完成标准）→ 通过则开放下一阶段 🔴 任务给对应角色认领；不通过则回退
```

项目经理关口检查指令示例：`接棒，审查[阶段X]产出，检查完成标准是否满足，满足则开放下一阶段任务`

---

## 🔙 回退棒（异常处理）

发现上一棒产出有问题：
1. 不覆盖上一棒工作
2. 在 `TASK_BOARD.md` 标记对应任务为 🚫
3. 在阻塞项写明问题
4. 更新 RELAY.md 跟踪表，创建「修复」标记指回上一角色
5. 告诉用户："阶段X产出有问题，请接修复棒"

---

## 🔀 并行棒（多角色同时推进）

前端和后端可同时开发，在 `TASK_BOARD.md` 中分别认领 B-02 / B-03（均 🔴），各自走 feature 分支；
两根棒都 ✅ 且 B-05 代码评审通过后 → 项目经理推进下一阶段。

---

## 📜 变更历史

| # | 时间 | 变更 |
|---|------|------|
| 0 | 07-30 | 初版接力棒机制 |
| 1 | 07-30 | 转为「既定任务跟踪表」+ 统一看板认领 + 保留 PM 关口检查（方案 A） |

---

## ⚙️ 使用规则（角色意图识别 — 3 分支）

角色会话收到消息，先判意图再行动：

| 你说了什么 | 角色判定 | 走哪条线 |
|------------|----------|----------|
| 「接棒 / 继续 / 该你 / 开始阶段X …」且当前处于该角色应认领的阶段任务 | 主线触发 | **看板认领**：去 TASK_BOARD 认领 🔴 任务 |
| 「接棒 / 继续 …」但当前阶段任务不属于我（跟踪表显示该阶段是别的角色） | **误用棒触发** | **两步不可省**：① 提示正确角色 ② 立即转即席，问是否需要我处理支线 |
| 对该角色的具体诉求（问需求、改接口、看文件…） | 即席请求 | **即席/直连模式** |
| 超出该角色权限 | 越权 | 标记阻塞/上报，不自行决定 |

### 误用棒触发 — 必须的两步（关键，之前漏掉）

```
第 1 步：明确提示
  "当前应由 [RELAY 跟踪表指向的角色] 处理 [阶段X] 任务，我是 [我的角色]，
   请用 [正确角色] 会话认领。"

第 2 步：立即转入即席模式（不能只拒绝就结束）
  "不过如果你有需要我 [我的角色] 处理的即席任务（问答/小改/看文件/评审…），
   请直接说，我会在权限范围内处理并轻量交付（commit+push+COMMLOG），不碰跟踪表。"
```

### 主线任务触发时

1. `git pull` → 读 RELAY.md 跟踪表确认当前阶段归属 → 读 TASK_BOARD.md
2. 认领 🔴 任务（状态 📋→🔄，填认领人）
3. 按角色权限工作
4. 完成：状态 → ✅，必要时在 TASK_BOARD 为下一角色创建/激活任务
5. 若动了产出物：`git commit + push` + COMMLOG 记录
6. 告知用户："[阶段X] 任务已完成，已为 [下一角色] 创建 [任务ID]，请其认领"

### 即席/直连模式

- 权限不变；纯问答直接答；动了产出物才 commit+push+COMMLOG；不更新 RELAY 跟踪表。
- 若即席任务确认该进主线，由项目经理登记进 TASK_BOARD。

---

> **最后更新**：2026-07-30 | **维护者**：项目经理 | **版本**：v2.0（跟踪表 + 统一看板认领 + 误用棒 3 分支）
