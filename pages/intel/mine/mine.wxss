/* INTEL-MODULE: AI 情报官 · 我的页样式（UI 重构 v6）。设计令牌全部引用 theme.json，零新增 hex。 */
.intel-mine {
  height: 100vh;
  background-color: var(--bg-page);
  display: flex;
  flex-direction: column;
  position: relative;
}
.status-bar-fill {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  width: 100%;
  z-index: 16;
}
.nav {
  display: flex;
  align-items: center;
  gap: 12rpx;
  padding: calc(var(--menu-top, 44px) + var(--menu-height, 32px) + 12rpx) 32rpx 8rpx;
}
.nav-back { font-size: 44rpx; color: var(--primary); line-height: 1; }
.nav-title { font-size: calc(36rpx * var(--font-scale)); font-weight: 700; color: var(--text-primary); }

.content {
  background-color: #F5F3F0;
  flex: 1;
  height: 0;
  padding: 16rpx 32rpx calc(16rpx + var(--safe-bottom, 0px));
  box-sizing: border-box;
  overflow-y: auto;
}
.page--dark .content { background-color: #000000; }

.sect-head {
  font-size: calc(26rpx * var(--font-scale));
  font-weight: 700;
  color: var(--text-secondary);
  margin: 28rpx 4rpx 16rpx;
}

/* 卡片 */
.card {
  background: var(--bg-card);
  border: 1rpx solid var(--divider);
  border-radius: 32rpx;
  padding: 28rpx;
  margin-bottom: 24rpx;
}
.card-action { display: flex; align-items: center; gap: 16rpx; }
.card-body { flex: 1; min-width: 0; }
.card-title {
  display: block;
  font-size: calc(30rpx * var(--font-scale));
  font-weight: 700;
  color: var(--text-primary);
}
.card-desc {
  display: block;
  font-size: calc(26rpx * var(--font-scale));
  color: var(--text-secondary);
  line-height: 1.6;
  margin-top: 10rpx;
}
.card-sub {
  display: block;
  font-size: calc(22rpx * var(--font-scale-meta, 1));
  color: var(--text-tertiary);
  margin-top: 10rpx;
}
.card-chev { color: var(--text-tertiary); font-size: calc(36rpx * var(--font-scale)); }

/* 撤销授权按钮 */
.revoke-btn {
  font-size: calc(24rpx * var(--font-scale-meta, 1));
  color: var(--color-favorite);
  border: 1rpx solid var(--color-favorite);
  border-radius: 28rpx;
  padding: 12rpx 24rpx;
  flex-shrink: 0;
}
.revoke-btn.disabled { color: var(--text-tertiary); border-color: var(--divider); }

.foot-note {
  text-align: center;
  font-size: calc(22rpx * var(--font-scale-meta, 1));
  color: var(--text-tertiary);
  margin: 8rpx 0 calc(16rpx + var(--safe-bottom, 0px));
}
