# 多会话协作协议（Multi-Session Collaboration Protocol）

> **适用场景**：一个项目由多个 AI 会话并行或串行协作完成。
> **核心理念**：文件即通信。所有会话通过 git 仓库 + 约定文件交换信息。

---

## 一、角色定义

| 角色 | 职责 | 标识 |
|------|------|------|
| **主会话（Orchestrator）** | 项目管理、任务分配、代码审查、联调验收、最终上线 | `[主]` |
| **开发会话（Developer）** | 功能开发、Bug 修复、测试编写 | `[dev]` |
| **测试会话（Tester）** | 测试用例编写、回归测试执行、Bug 报告 | `[test]` |
| **文档会话（Docs）** | 文档编写、SOP 维护、项目日志更新 | `[docs]` |

> 主会话是唯一有权限合并分支和推送到 main 的角色。其他会话通过 PR/MR 交付。

---

## 二、核心约定文件

每个会话启动后，**必须按顺序**读取以下文件以获取项目上下文：

| 顺序 | 文件 | 内容 | 频率 |
|------|------|------|------|
| 1 | `CONTEXT.md` | 项目速览、当前状态、待办优先级 | 每次启动 |
| 2 | `TASK_BOARD.md` | 任务看板，知道谁在做什么 | 每次启动 |
| 3 | `COLLABORATION.md`（本文件） | 协作协议本身 | 首次启动 |
| 4 | `docs/项目日志.md` | 完整项目历史（按需） | 需要了解背景时 |
| 5 | `docs/SOP-软件开发流程基准.md` | 开发规范（按需） | 需要了解规范时 |

---

## 三、会话生命周期

### 3.1 启动阶段

```
1. git pull origin main          # 拉取最新代码
2. 读取 CONTEXT.md               # 了解项目状态
3. 读取 TASK_BOARD.md            # 认领任务
4. 创建功能分支                  # git checkout -b feature/xxx
5. 更新 TASK_BOARD.md            # 将任务状态改为 🔄 进行中
6. 开始工作
```

### 3.2 开发阶段

```
1. 遵循 docs/SOP-软件开发流程基准.md 中的规范
2. 遵循 CONTEXT.md 中的「关键开发红线」
3. 小步提交，commit message 格式：type: 简短英文描述
4. 涉及关键决策时，在 COMMLOG.md 中记录
```

### 3.3 交付阶段（会话结束前必须执行）

```
1. 运行回归测试                    # node test/v*-regression-*.js
2. 更新 TASK_BOARD.md              # 将任务状态改为 ✅ 已完成
3. 更新 CONTEXT.md                 # 更新「当前状态」和「最新提交」
4. 更新 docs/项目日志.md           # 记录本次变更
5. 在 COMMLOG.md 追加交接说明      # 遗留问题、注意事项
6. git add + commit + push origin feature/xxx
7. 通知主会话进行代码审查和合并
```

---

## 四、并行协作规则

### 4.1 避免冲突

| 规则 | 说明 |
|------|------|
| **文件隔离** | 不同会话操作不同文件/目录，天然无冲突 |
| **同文件操作** | 必须通过 git 分支隔离，由主会话合并解决冲突 |
| **公共文件修改** | 修改 `CONTEXT.md`、`TASK_BOARD.md` 等公共文件时，先 pull 再改 |

### 4.2 依赖管理

```
功能 B 依赖功能 A：
  1. A 会话完成 → 合并到 main
  2. B 会话 git rebase main → 继续开发

功能 A 和功能 B 并行且需联调：
  1. 各自在独立分支开发
  2. 都完成后，主会话创建联调分支 merge 两者
  3. 测试会话在联调分支上执行集成测试
```

### 4.3 状态同步

- 每个会话通过 `TASK_BOARD.md` 了解其他会话的进度
- 发现阻塞时，在 `TASK_BOARD.md` 的「阻塞项」区域标记
- 主会话负责协调解阻塞

---

## 五、联调验收流程

```
1. 所有功能分支开发完成
2. 主会话创建 release/x.y.z 分支
3. 将所有功能分支合并到 release 分支
4. 测试会话执行全量回归测试
5. 主会话执行验收清单
6. 通过后合并到 main → 打 tag → push
7. 更新 CHANGELOG.md
8. 更新 tdrive 项目资产库文档
```

---

## 六、沟通记录 COMMLOG.md

每次会话结束时，在 `COMMLOG.md` 追加一条记录：

```markdown
## [2026-07-28] 功能：阅读模式 | 会话：dev-reading-mode

### 完成内容
- 详情页上下滑切换新闻（跨分类串联）
- 45 项回归测试通过

### 变更文件
- pages/detail/detail.js — 新增 readingList 构建逻辑
- pages/detail/detail.wxml — 替换为 swiper 结构
- test/v8-regression-reading-mode-v2.js — 新增

### 遗留问题
- 无

### 注意事项
- swiper 的 bindchange 在快速滑动时可能丢帧，后续可考虑 WXS 手势
```

---

## 七、Git 分支规范

| 分支类型 | 命名 | 示例 |
|----------|------|------|
| 功能开发 | `feature/<功能名>` | `feature/reading-mode` |
| Bug 修复 | `fix/<问题简述>` | `fix/wxs-touch-block` |
| 发布分支 | `release/x.y.z` | `release/1.0.0` |
| 联调分支 | `integration/<描述>` | `integration/v8-all-features` |

---

## 八、任务看板使用说明

`TASK_BOARD.md` 是实时协作的核心：

- **认领任务**：把任务状态从 `📋` 改为 `🔄`，写上自己的会话标识
- **完成任务**：把状态改为 `✅`，附上 commit hash
- **遇到阻塞**：把状态改为 `🚫`，在「阻塞项」区域写明原因
- **新增任务**：任何人都可以添加，主会话负责排优先级

---

> **本协议版本**：v1.0 | **生效日期**：2026-07-28
> **维护者**：主会话 | **下次审查**：项目结束时
