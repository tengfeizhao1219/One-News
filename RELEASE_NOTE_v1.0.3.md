# One News · Release Note v1.0.3

> 发布日期：2026-08-26 ｜ 上一版本：v1.0.2 ｜ 类型：AI 情报官模块大幅打磨 + One News 体验升级 + 工程化

---

## 🎯 重点：AI 情报官模块（AI Intel Officer）全面打磨

### 一、信息阅读体验

- **顶部阅读进度条（全新）**：去掉右侧边滚动条，改为 One News 同款的**顶部 3rpx 浅蓝进度线**，且精确定位到顶部状态区域底部——阅读位置一眼可见，滚动时进度实时更新（做了节流防卡顿）。
- **深挖交互优化**：展开「深挖」面板时**仅固定标题行**，meta（来源 + 时间 + 收藏）随正文一起上滑——正文不再被标题遮挡，交互更自然。
- **顶部区域紧凑化**：删除标题与导航之间那段留白，顶部状态区往下扩展约 60%，页面更紧凑、无突兀白条。
- **标题背景 = 页面色**：平时 / 深挖展开都保持不透明，背景用页面同色（`bg-card`），不额外加底色块——既防正文透过重叠，又和正文一色融合。

### 二、收藏体系

- 收藏页移除每条「已收藏」红心标识——本页即已收藏列表，无需重复提醒。
- 卡片**左右对称留白**（修复 scroll-view `padding-right` 失效导致的右侧贴边），长文本 word-break 换行防溢出。
- 首页摘要字体调大（`card-desc` 26→30rpx），更易读。

### 三、界面干净

- **侧边滚动条全部隐藏**（AI 情报官首页 / 详情页），覆盖 `::-webkit-scrollbar` + `scrollbar-width` 所有渲染引擎，首次进入也不再出现。
- 状态栏 / 导航 / 滚动容器样式统一收口，观感一致。

---

## 🛠 One News 模块

- **news_cache 注入前跨源去重（owner 拍板）**：publish 前与现有缓存比较（URL 归一化 / 标题指纹 / 标题包含关系），重复条目从当前批次剔除后再全量替换注入；空批保护（剔除后为空则保留现有数据）——不同源转载同一新闻不再被重复展示。
- **详情页搜索按钮 / 底部操作栏修整**：搜索按钮改 `fixed`(不与操作栏重叠、任何设备稳定) + safe-area 适配；操作栏横排压缩到 40rpx、icon 40rpx。
- **移除详情页底部应用来源归属、滑动提示条**（更简洁）。
- **详情页正文字号缩放一档**：引入页面级 `--page-fs:0.875`，正文/标题/meta 等比缩小（正文 32→28rpx），仅字号不动行高。
- 首页「数据截至」已移除；首页滚动条隐藏；标题底色调整。

---

## 🔧 工程化 / 协作

- **CloudBase MCP 部署链路**：云函数经官方 MCP(SCF) 部署，免手动。
- **微信开发者工具 skill-cli(wechatide) 自动化**：上传/预览/截图/识图/云开发全可 agent 直接操作；`auto_preview` 推送微信、`create_preview_qrcode` 二维码。
- **每日 23:00 git 自动提交**（本地工作副本 + 计划任务）+ 多会话 `file-lock` 文件锁防覆盖。
- 识图能力：ys365 视觉模型 + 官方多模态(`deepseek-v4-flash-vision-exp`) 双通道。

---

## ✅ 验证

- One News / AI 情报官详情页正文字号统一（32rpx），详情页整体 0.875 缩放生效。
- AI 情报官首页/详情页/收藏页侧边滚动条均隐藏。
- 模拟器无死循环/长时间无响应（节流修复）。

---

## 📋 上线说明

- 小程序前端：本包为**前端**改动（`upload` 上传体验版即可），不涉及云函数代码变更，无需重新部署云函数（去重逻辑此前已线上验证）。
- 若发现标题/正文区域仍显色差，检查是否新增硬编码色值 → 应仅用 `theme.json` token。
- 版本号以线上实际发布版本为准（本版 v1.0.3，基于 v1.0.2 递增）。
