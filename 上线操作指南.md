# 「一页」小程序 — 上线操作指南

> 你只需要按这个文档一步步点击，其他事情交给我。

---

## 准备工作（现在就可以做）

### 你需要给我的信息

请把以下两个信息告诉我：

| 信息 | 在哪找 | 格式示例 |
|------|--------|---------|
| **小程序 AppID** | 微信公众平台 → 开发管理 → 开发设置 | `wx1234567890abcdef` |
| **云开发环境 ID** | 开通云开发后会显示（见下方步骤3） | `your-env-xxxxx` |

---

## 操作步骤

### 第 1 步：下载微信开发者工具

1. 打开浏览器访问：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
2. 下载对应系统的**稳定版**
3. 安装后打开，用你的微信扫码登录

---

### 第 2 步：导入项目

1. 微信开发者工具首页 → 点击 **「导入项目」** 或 **「+」**
2. 填写信息：

| 字段 | 填写内容 |
|------|---------|
| 项目名称 | `一页` |
| 目录 | **选择 `/workspace/quick-news-miniapp` 文件夹**（即项目根目录） |
| AppID | 填入你的小程序 AppID |

3. 点击「确定」

---

### 第 3 步：开通云开发

1. 在开发者工具顶部菜单栏 → 点击 **「云开发」** 图标（云朵形状）
2. 点击「开通」→ 选择「按量付费」或「免费额度」→ 确认
3. 开通成功后，页面顶部会显示**环境 ID**（类似 `your-env-xxxxx`）
4. ⚠️ **把这个环境 ID 发给我**

---

### 第 4 步：替换配置（✅ AppID 已完成）

- ✅ `project.config.json` 中的 AppID 已替换为 `wx1ccb4d171dd88162`
- ⬜ 等你的云开发环境 ID → 我会更新 `app.js`
- ⬜ 切换 Mock 模式为云函数模式

你只需要在开发者工具中**刷新项目**即可。

---

### 第 5 步：部署云函数（在开发者工具中操作）

1. 在左侧文件列表找到 `cloudfunctions/` 目录
2. 右键 `getNewsList` → 选择 **「上传并部署：云端安装依赖」**
3. 等待上传完成（约 30 秒）
4. 同样操作 `getNewsDetail` 和 `searchNews`
5. 完成后，3 个云函数图标会变成**云朵+绿色勾**

---

### 第 6 步：导入新闻数据

1. 点击顶部「云开发」→ 进入云开发控制台
2. 左侧菜单 → **「数据库」**
3. 点击「添加集合」→ 集合名称输入 `news` → 确定
4. 点击 `news` 集合 → 「导入」按钮
5. 选择 JSON 格式导入
6. 导入文件：选择项目中的 `cloud-import-data.json`（已为你生成好）

---

### 第 7 步：预览测试

1. 开发者工具顶部 → 点击 **「预览」**
2. 手机微信扫码 → 在手机上测试
3. 测试内容：
   - 上下滑动切换新闻
   - 左滑呼出分类面板
   - 切换分类
   - 点击卡片进入详情
   - 搜索功能
   - 暗色模式

---

### 第 8 步：上传发布

1. 确认测试通过后 → 点击顶部 **「上传」**
2. 版本号填 `1.0.0`，描述写 `MVP 版本上线`
3. 上传完成后 → 登录 [微信公众平台](https://mp.weixin.qq.com/)
4. 左侧菜单 → **版本管理**
5. 找到刚上传的版本 → 点击「提交审核」
6. 配置：
   - 类目：工具 → 信息查询
   - 标签：新闻、阅读、资讯
7. 提交后等待审核（通常 1-3 个工作日）

---

## 我现在需要你做的

1. **把 AppID 发给我**
2. **下载微信开发者工具**
3. **导入项目**（按第 2 步操作）
4. **开通云开发**（按第 3 步操作），把环境 ID 发给我

然后我会立即帮你完成第 4 步的配置替换，剩下的部署、导入数据我会继续一步步指导。

---

> 💡 如果你在操作中遇到任何报错或不知道点哪里，**截图发给我**，我来帮你排查。
